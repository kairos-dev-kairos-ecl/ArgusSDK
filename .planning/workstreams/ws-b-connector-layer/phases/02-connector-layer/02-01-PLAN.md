---
plan: 02-01
phase: 2
wave: 1
depends_on: []
files_modified:
  - internal/ocsf/mapper.go
  - internal/ocsf/mapper_test.go
autonomous: true
requirements:
  - SC-2  # internal/ocsf/ maps all 14 ArgusSignal fields to OCSF v1.3 — unit-tested round-trip

must_haves:
  truths:
    - "go test ./internal/ocsf/... passes with no skips"
    - "Every signal.Layer constant (L1–L10, LDecision) is covered by a test case in mapper_test.go"
    - "MapBatch returns a non-nil Event for every layer constant with no errors on a well-formed Signal"
    - "All 14 Signal fields (SignalID, TraceID, SpanID, ParentSpanID, Layer, Category, Severity, AppID, AppVersion, SDKVersion, Env, Timestamp, DurationMS, ContextJSON) are verifiably present in Event output"
    - "L9APIGateway produces an Event with non-nil HttpRequest.URL, HttpRequest.Method, HttpResponse.Code, and DstEndpoint"
    - "L7RAGRetrieval produces an Event with non-nil SrcEndpoint"
    - "L6Safety and LDecision produce an Event with non-nil FindingInfo.UID and FindingInfo.Title"
    - "System-class layers (L1, L2, L4) produce an Event with non-nil Device.Hostname"
    - "ContextJSON round-trip: a JSON blob with known keys survives into Event.Unmapped['context']"
  artifacts:
    - path: "internal/ocsf/mapper_test.go"
      provides: "Unit tests for all 11 layer mappings and ContextJSON round-trip"
      min_lines: 150
    - path: "internal/ocsf/mapper.go"
      provides: "Completed populateClassFields with ContextJSON parsing stubs removed (real extraction)"
      contains: "func (m *Mapper) Map"
---

<objective>
Complete and unit-test the OCSF canonical mapper (internal/ocsf/mapper.go).

The mapper is already substantially scaffolded — all types, ClassUID constants, layerToClassUID, layerToActivityID, and the Map/MapBatch methods exist. What is missing:
1. Unit tests covering all 11 layer-to-class mappings and all 14 Signal fields.
2. Real ContextJSON field extraction replacing the TODO comments in populateClassFields (specifically: L9APIGateway URL/method/status from context, L7RAGRetrieval databucket name from context, L10Application web_resources from context).

Purpose: The OCSF mapper is the canonical translation layer consumed by every connector (Kafka, Splunk, Elastic). Tests lock in correctness before connectors are implemented.
Output: mapper_test.go with >150 lines of deterministic test coverage; mapper.go with ContextJSON extraction implemented for L9, L7, L10.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/ws-b-connector-layer/ROADMAP.md
@.planning/workstreams/ws-b-connector-layer/phases/02-connector-layer/02-RESEARCH.md
@pkg/signal/signal.go
</context>

<interfaces>
From internal/ocsf/mapper.go (read before coding):

Key types already defined:
- ClassUID constants: ClassMemoryActivity=1004, ClassModuleActivity=1005, ClassAPIActivity=6003, ClassDetectionFinding=2004, ClassHTTPActivity=4002, ClassWebResourcesActivity=6001, ClassDatastoreActivity=6005
- Event struct with fields: ClassUID, ClassName, CategoryUID, ActivityID, TypeUID, Time, SeverityID, Metadata, HttpRequest, HttpResponse, Api, FindingInfo, SrcEndpoint, DstEndpoint, Device, ActionID, Actor, Unmapped
- Mapper struct with productVersion, agentHostname
- NewMapper(productVersion, agentHostname string) *Mapper
- func (m *Mapper) Map(s signal.Signal) (*Event, error)
- func (m *Mapper) MapBatch(signals []signal.Signal) ([]*Event, []error)

Layer → ClassUID mapping (verified in mapper.go):
- L1Hardware, L4Transformer → ClassMemoryActivity (1004) — device required
- L2ModelWeights → ClassModuleActivity (1005) — device required
- L3Tokenizer, L5OutputDecoding, L8Agents → ClassAPIActivity (6003) — api + src_endpoint required
- L6Safety, LDecision → ClassDetectionFinding (2004) — finding_info required
- L9APIGateway → ClassHTTPActivity (4002) — http_request + http_response + dst_endpoint required
- L10Application → ClassWebResourcesActivity (6001)
- L7RAGRetrieval → ClassDatastoreActivity (6005) — src_endpoint required

Signal fields (14 total, from pkg/signal/signal.go):
SignalID, TraceID, SpanID, ParentSpanID, Layer, Category, Severity, AppID, AppVersion, SDKVersion, Env, Timestamp, DurationMS, ContextJSON

ContextJSON TODOs to resolve (populateClassFields):
- ClassHTTPActivity: parse url, method, status_code from ContextJSON; fall back to defaults if absent
- ClassDatastoreActivity: parse vector_index key → databucket.name
- ClassWebResourcesActivity: parse resource details for web_resources field
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unit tests for all 11 layer mappings and 14 Signal fields</name>
  <files>internal/ocsf/mapper_test.go</files>
  <read_first>
    - internal/ocsf/mapper.go (full file — understand every exported type and method before writing tests)
    - pkg/signal/signal.go (all 14 Signal fields and Layer/Severity constants)
  </read_first>
  <behavior>
    - TestMap_AllLayers: table-driven test with one row per Layer constant (L1–L10, LDecision). For each: verify class_uid matches expected ClassUID, category_uid derived correctly, activity_id non-zero, type_uid == int(class_uid)*100 + activity_id, metadata.version == "1.3.0", metadata.product.vendor_name == "Argus", metadata.uid == signal.SignalID, time == signal.Timestamp.UnixMilli(), severity_id == int(signal.Severity).
    - TestMap_AllSignalFields: single signal with all 14 fields set to non-zero values. Verify: SignalID → metadata.uid, TraceID → unmapped["trace_id"], SpanID → unmapped["span_id"], Layer → class_uid (via mapping), Category → unmapped["category"] OR class-specific field, Severity → severity_id, AppID → actor.app_uid, AppVersion → (not required in OCSF, but verify no panic), SDKVersion → unmapped["sdk_version"], Env → not required (no panic), Timestamp → ev.Time == ts.UnixMilli(), DurationMS → no panic, ContextJSON → unmapped["context"] present when valid JSON.
    - TestMap_SystemClassDevice: L1, L2, L4 produce non-nil Device with Hostname == agentHostname.
    - TestMap_DetectionFinding: L6Safety and LDecision produce non-nil FindingInfo with non-empty UID and Title; L6Safety analytic.type_id == 4; LDecision analytic.type_id == 1.
    - TestMap_HTTPActivity: L9APIGateway produces non-nil HttpRequest, HttpResponse, DstEndpoint.
    - TestMap_APIActivity: L3, L5, L8 produce non-nil Api with non-empty Operation, non-nil SrcEndpoint.
    - TestMap_DatastoreActivity: L7RAGRetrieval produces non-nil SrcEndpoint.
    - TestMapBatch_ErrorHandling: unknown layer (Layer=99) returns error for that signal; valid signals in same batch still produce events.
    - TestMap_ContextJSON_RoundTrip: signal with ContextJSON=`{"url":"https://api.example.com","method":"GET","status_code":200}` for L9; verify unmapped["context"] contains the parsed object.
    - TestMap_InvalidSeverity: severity 0 (unspecified) maps to severity_id=1 (Informational) per the guard in Map().
  </behavior>
  <action>
    Create internal/ocsf/mapper_test.go with package ocsf (white-box test, same package for access to unexported helpers).

    Import path: "github.com/kairos-dev-kairos-ecl/ArgusSDK/pkg/signal".
    Use testing.T and standard library only — no testify (not in go.mod).

    Table-driven test TestMap_AllLayers: define a []struct{layer signal.Layer; wantClassUID ClassUID; wantCategoryUID int; wantActivityID int} with 11 rows. Call NewMapper("1.0.0", "test-host") and Map() for each. Use t.Run(fmt.Sprintf("layer_%d", tc.layer), ...) for subtests.

    TestMap_AllSignalFields: construct a signal.Signal with all 14 fields populated (use time.Unix(1700000000, 0) for Timestamp, ContextJSON=[]byte(`{"key":"val"}`), Severity=signal.SeverityHigh). Call Map(). Assert: ev.Metadata.UID == signal.SignalID, ev.Actor.AppUID == signal.AppID, ev.Unmapped["trace_id"] == signal.TraceID, ev.Unmapped["span_id"] == signal.SpanID, ev.Unmapped["sdk_version"] == signal.SDKVersion, ev.Time == signal.Timestamp.UnixMilli(), ev.SeverityID == int(signal.SeverityHigh).

    Use t.Fatal on nil pointer before field access to produce readable failures.
  </action>
  <verify>
    <automated>cd C:\Users\Drupad\argus-sdk && go test ./internal/ocsf/... -v -count=1 -run TestMap 2>&1</automated>
  </verify>
  <done>go test ./internal/ocsf/... passes. All 11 layer subtests pass. TestMap_AllSignalFields and TestMap_ContextJSON_RoundTrip pass. No panics on nil pointer access.</done>
</task>

<task type="auto">
  <name>Task 2: Complete ContextJSON extraction in populateClassFields</name>
  <files>internal/ocsf/mapper.go</files>
  <read_first>
    - internal/ocsf/mapper.go (lines 336–395: populateClassFields and its TODO comments)
    - pkg/signal/signal.go (ContextJSON is []byte, raw JSON blob)
  </read_first>
  <action>
    Replace the three TODO comments in populateClassFields with real ContextJSON extraction using json.Unmarshal into a map[string]interface{}.

    For ClassHTTPActivity (L9APIGateway): unmarshal s.ContextJSON into a local ctx map. If "url" key present and non-empty string, use it as ev.HttpRequest.URL; otherwise keep s.Category as the fallback. If "method" key present, use it as ev.HttpRequest.Method; otherwise keep "POST". If "status_code" key present and is float64, cast to int and set ev.HttpResponse.Code.

    For ClassDatastoreActivity (L7RAGRetrieval): unmarshal s.ContextJSON into ctx map. If "vector_index" key present and non-empty string, add a Databucket field — but since Databucket is not yet on Event struct, store it in Unmapped["databucket_name"] for now. Add a comment: "TODO: promote to first-class databucket object when OCSF databucket type is added to Event."

    For ClassWebResourcesActivity (L10Application): unmarshal s.ContextJSON into ctx map. If "resource_url" key present, store in Unmapped["web_resource_url"]. Add a comment noting web_resources object is deferred pending OCSF extension work.

    All unmarshal calls must be defensive: if s.ContextJSON is nil or unmarshal fails, fall through to defaults silently (no error returned). Use a local helper function extractCtx(contextJSON []byte) map[string]interface{} that returns nil on failure.

    Do NOT remove or alter any existing code outside populateClassFields. Do NOT add new imports beyond what already exists (encoding/json is already imported).
  </action>
  <verify>
    <automated>cd C:\Users\Drupad\argus-sdk && go test ./internal/ocsf/... -v -count=1 2>&1</automated>
  </verify>
  <done>All ocsf tests pass including TestMap_ContextJSON_RoundTrip. go vet ./internal/ocsf/... produces no output. L9APIGateway with ContextJSON={"url":"https://api.example.com","method":"GET","status_code":200} produces HttpRequest.URL=="https://api.example.com", HttpRequest.Method=="GET", HttpResponse.Code==200.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ContextJSON input | User-controlled JSON blob from instrumentation libs; parsed in populateClassFields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | populateClassFields ContextJSON parsing | mitigate | Always unmarshal into local map; never eval or execute parsed content; fall through to defaults on any parse error |
| T-02-02 | Information Disclosure | Unmapped["context"] in OCSF output | accept | ContextJSON is already written to Unmapped in Map(); this plan preserves the existing behaviour and adds no new disclosure surface |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | This plan adds no new dependencies; no package install tasks |
</threat_model>

<verification>
go test ./internal/ocsf/... -v -count=1
go vet ./internal/ocsf/...
</verification>

<success_criteria>
- mapper_test.go exists with at least 150 lines
- All 11 layer constants covered in TestMap_AllLayers subtests
- All 14 Signal fields verified in TestMap_AllSignalFields
- TestMap_ContextJSON_RoundTrip verifies L9 URL/method/status extraction from ContextJSON
- go test ./internal/ocsf/... exits 0
</success_criteria>

<output>
Create .planning/workstreams/ws-b-connector-layer/phases/02-connector-layer/02-01-SUMMARY.md when done
</output>
