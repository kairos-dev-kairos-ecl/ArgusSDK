---
plan: 03-03
phase: 3
wave: 2
# Sequential-execution ordering note: this plan and 03-02 both modify
# internal/connector/elastic/connector.go. Plans on this machine execute
# SEQUENTIALLY on the main tree (no worktree isolation — see 03-CONTEXT.md
# Test Environment Notes), so this is safe, but 03-03 MUST run after 03-02.
depends_on:
  - 03-02
files_modified:
  - internal/connector/elastic/connector.go
  - internal/connector/elastic/connector_test.go
  - internal/connector/config.go
  - internal/connector/config_test.go
  - internal/resilience/circuit_breaker.go
  - internal/resilience/circuit_breaker_test.go
  - internal/secrets/store.go
  - internal/secrets/env_fallback.go
  - internal/secrets/store_test.go
  - internal/connector/kafka/connector.go
  - internal/connector/kafka/connector_test.go
  - internal/ocsf/mapper.go
  - internal/ocsf/mapper_test.go
autonomous: true
requirements:
  - SC-5   # Elastic /_bulk action line built via json.Marshal — no string concatenation of cfg.Index
  - SC-9   # Config Watcher survives atomic-rename replacement (watches parent dir, filters filename, handles Rename/Remove) and logs via zap
  - SC-10  # CircuitBreaker.Call holds the mutex for the full state-transition decision (no TOCTOU)
  - SC-11  # Medium/low: GetSecret caches decrypted map; Kafka RequiredAcks sentinel for "unset"; ActivityName set when activity_id==99; secrets temp file 0600

must_haves:
  truths:
    - "Elastic /_bulk action line is produced by json.Marshal of a struct/map containing cfg.Index — a hostile index name containing quotes/newlines cannot inject bulk-API parameters (F4)"
    - "Raw cfg.APIKey is zeroed on the stored Connector config after apiKeyHeader is computed; Connect's empty-check uses apiKeyHeader (F16)"
    - "Config Watcher watches the PARENT DIRECTORY of the config file, filters events by target filename, and reloads on Write/Create/Rename of the target — an atomic-rename save (write temp + rename over target) triggers onChange (F9)"
    - "Watcher logs via an injected *zap.Logger (default zap.NewNop()) — no fmt.Fprintf(os.Stderr) remains in config.go (F9)"
    - "CircuitBreaker.Call performs the open-state check, timeout comparison, and Open->HalfOpen transition under a single held cb.mu — two concurrent callers cannot both transition; lastFailureTime is never read unlocked (F10)"
    - "GetSecret served from a cached decrypted map behind sync.RWMutex; SaveSecrets invalidates the cache; the store file is NOT re-read+re-decrypted on every GetSecret call (F11)"
    - "Secrets temp file is created with os.OpenFile(..., 0600) — never momentarily world-readable via os.Create (F15)"
    - "kafka Config distinguishes unset RequiredAcks (nil => default 1) from explicit 0 (NoAck) — kgo.NoAck() is reachable (F12)"
    - "Mapper.Map sets ev.ActivityName=\"Other\" whenever activityID==99 (F13)"
    - "F17 (LoggedTime non-determinism) is documented as accepted/deferred — code comment present, NOT fixed (locked decision 5)"
  artifacts:
    - path: "internal/connector/elastic/connector.go"
      provides: "json.Marshal-built action line; APIKey zeroed after header computation"
      contains: "json.Marshal"
    - path: "internal/connector/config.go"
      provides: "Parent-directory watcher with filename filter, Rename/Remove handling, zap logging"
      contains: "zap"
    - path: "internal/resilience/circuit_breaker.go"
      provides: "TOCTOU-free Call: single-lock state decision"
      min_lines: 150
    - path: "internal/secrets/store.go"
      provides: "Cached decrypted secrets behind RWMutex; 0600 temp file"
      contains: "O_CREATE"
    - path: "internal/connector/kafka/connector.go"
      provides: "RequiredAcks *int with nil=default-1 semantics"
      contains: "RequiredAcks *int"
    - path: "internal/ocsf/mapper.go"
      provides: "ActivityName=Other for activity_id 99; F17 deferral comment"
      contains: "ActivityName"
---

<objective>
Close the injection finding (F4) and the remaining infra findings (F9, F10, F11, F12, F13, F15, F16); document F17 as accepted/deferred.

- F4 (critical, security): elastic action line at connector.go:217 is built by string concatenation of cfg.Index. A hot-reloadable config value containing `"` injects /_bulk API parameters. Fix with json.Marshal.
- F16 (low, security): elastic retains the raw APIKey on c.cfg after computing apiKeyHeader.
- F9 (high): config Watcher only handles Write|Create on the file inode; atomic-rename saves (vim, temp+rename writers) silently kill the watch. Also logs to stderr instead of zap.
- F10 (medium): CircuitBreaker.Call TOCTOU — state read under lock then released; lastFailureTime read unlocked; two goroutines can both transition Open->HalfOpen.
- F11 (medium): GetSecret re-reads and re-decrypts the entire store file on every call.
- F15 (low): secrets temp file created via os.Create (0666&umask) before the later Chmod — perms window.
- F12 (low): kafka `if cfg.RequiredAcks == 0 { cfg.RequiredAcks = 1 }` makes NoAck unreachable.
- F13 (low): OCSF activity_id=99 emitted without ActivityName; validators may reject "Other" without a name.
- F17 (accepted): mapper LoggedTime time.Now() non-determinism — DEFER per locked decision 5; document only.

Constraints (from 03-CONTEXT.md, binding):
- No new dependencies (zap and fsnotify are EXISTING deps — config.go already imports fsnotify, connector.go already imports zap).
- No proto / pkg/signal API changes. No agent wiring.
- Windows: tests must Close() file handles before delete/rename.
- Sequential execution; this plan runs AFTER 03-02 (shared file: elastic/connector.go).

Purpose: F4 is the one remote-injection sink found in review; the rest are correctness/hygiene debts that block the REQUEST CHANGES verdict from clearing.
Output: eight findings fixed with tests; one finding documented-deferred.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/ws-b-connector-layer/ROADMAP.md
@.planning/workstreams/ws-b-connector-layer/phases/03-review-remediation/03-CONTEXT.md
@internal/connector/elastic/connector.go
@internal/connector/config.go
@internal/resilience/circuit_breaker.go
@internal/secrets/store.go
@internal/secrets/env_fallback.go
@internal/connector/kafka/connector.go
@internal/ocsf/mapper.go
</context>

<interfaces>
Current code the executor must know (verified against source 2026-06-10):

F4/F16 — internal/connector/elastic/connector.go:
  Line 217: actionLine := []byte(`{"index":{"_index":"` + c.cfg.Index + `"}}` + "\n")   // INJECTION SINK
  New(cfg Config) at 88-112: computes apiKeyHeader = "ApiKey " + base64(cfg.APIKey) but stores cfg (with raw APIKey) on c.cfg.
  Connect at 133: `if c.cfg.APIKey == "" { return ... }` — must become apiKeyHeader check after F16 zeroing.
  NOTE: 03-02 has already restructured Send into chunked sendChunk by the time this plan runs — apply F4 to wherever the action line now lives.

F9 — internal/connector/config.go:
  NewWatcher(path string, onChange func(*ConnectorsFileConfig)) (*Watcher, error)   // fw.Add(path) — watches the FILE inode
  Watcher { path string; onChange func(...); watcher *fsnotify.Watcher }
  Start(ctx): handles only event.Has(fsnotify.Write) || event.Has(fsnotify.Create); errors via fmt.Fprintf(os.Stderr, ...) at lines 97 and 106.
  LoadConnectorsConfig(path) (*ConnectorsFileConfig, error) — unchanged; malformed YAML keeps previous config (T-02-03 invariant to preserve).
  Package connector already imports go.uber.org/zap (connector.go).

F10 — internal/resilience/circuit_breaker.go:
  Call(operation func() error) error — lines 58-74: cb.mu.Lock/read state/Unlock; then `time.Since(cb.lastFailureTime)` read UNLOCKED at 65; re-locks to transition to HalfOpen. Post-operation result handling (lines 81-126) already holds cb.mu correctly.
  Fields guarded by mu: state, failures, successes, lastFailureTime, lastStateChange. Metrics via atomic int64s.

F11/F15 — internal/secrets:
  Store { path string; masterKey []byte }
  (s *Store) LoadSecrets() (map[string]string, error)   // full read+AES-GCM decrypt per call
  (s *Store) SaveSecrets(map[string]string) error        // store.go:156: f, err := os.Create(tmpPath)  <- F15; later os.Chmod(s.path, 0600)
  env_fallback.go: GetSecret(key) calls s.LoadSecrets() on EVERY call (line 36) — F11. storePtr atomic.Pointer[Store]; SetStore(s).

F12 — internal/connector/kafka/connector.go:
  Config.RequiredAcks int  // doc: 0=none, 1=leader, -1=all. Default: 1
  New() lines 81-83: if cfg.RequiredAcks == 0 { cfg.RequiredAcks = 1 }   // makes NoAck unreachable
  Connect() lines 116-123: switch c.cfg.RequiredAcks { case -1: AllISRAcks; case 0: NoAck (DEAD CODE today); default: LeaderAck }

F13/F17 — internal/ocsf/mapper.go:
  Event.ActivityName string `json:"activity_name,omitempty"` (line 87 — field exists, never set)
  Map(s signal.Signal) (*Event, error) at 270; activityID := layerToActivityID(s.Layer) at 281; Event literal at 287-317.
  layerToActivityID returns 99 for signal.L8Agents (line 518).
  Line 304: LoggedTime: time.Now().UTC()  — F17, DEFERRED: comment only.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Elastic — F4 injection fix + F16 APIKey zeroing</name>
  <files>internal/connector/elastic/connector.go, internal/connector/elastic/connector_test.go</files>
  <read_first>
    - internal/connector/elastic/connector.go (full — note Send is chunked after 03-02)
    - internal/connector/elastic/connector_test.go (httptest patterns; tests added by 03-02)
  </read_first>
  <behavior>
    - TestSend_HostileIndexNameStaysWellFormed (F4): cfg.Index = `evil"}},{"delete":{"_index":"x` (quote/brace injection). Capture the /_bulk request body in an httptest server. Assert every odd NDJSON line parses as JSON whose only top-level key is "index" and whose `index._index` string EQUALS the hostile value verbatim (i.e. it was escaped, not interpreted); assert the body contains no `"delete"` action object as a separate JSON document.
    - TestNew_APIKeyZeroedAfterHeader (F16): white-box (package elastic) — after New(Config{APIKey:"id:key",...}), c.cfg.APIKey == "" and c.apiKeyHeader == "ApiKey " + base64("id:key").
    - TestConnect_EmptyAPIKeyStillRejected (F16 regression): New with empty APIKey → Connect returns the "APIKey is required" error (now via apiKeyHeader == "" check).
  </behavior>
  <action>
    F4: replace the string-concatenated action line with json.Marshal(map[string]any{"index": map[string]string{"_index": c.cfg.Index}}) + "\n". Compute it ONCE per Send (or precompute in New and recompute if cfg changes — per 03-CONTEXT F4 fix text); a Marshal error on a plain string map is effectively impossible but handle it by returning a failed ack + non-nil error (the 03-02 contract).
    F16: in New, after computing apiKeyHeader, set cfg.APIKey = "" BEFORE assigning c.cfg. Change Connect's check from c.cfg.APIKey == "" to c.apiKeyHeader == "" (keep the same error message). Verify no other reads of c.cfg.APIKey remain (grep).
    Commits: test(03-03): hostile-index injection + APIKey retention tests (F4,F16) then fix(03-03): json.Marshal action line, zero raw APIKey (F4,F16).
  </action>
  <verify>
    <automated>cd C:\Users\Drupad\argus-sdk && go test -race ./internal/connector/elastic/... -count=1 -timeout 120s 2>&1</automated>
  </verify>
  <done>Hostile index name cannot alter /_bulk structure; no string concatenation of cfg.Index remains; raw APIKey absent from the stored struct; all elastic tests (including 03-02's) still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Config Watcher rename-survival + zap (F9); CircuitBreaker TOCTOU (F10)</name>
  <files>internal/connector/config.go, internal/connector/config_test.go, internal/resilience/circuit_breaker.go, internal/resilience/circuit_breaker_test.go</files>
  <read_first>
    - internal/connector/config.go (full)
    - internal/connector/config_test.go (existing watcher tests — NewWatcher call sites must be updated for the new signature)
    - internal/resilience/circuit_breaker.go (Call at lines 58-127)
    - internal/resilience/circuit_breaker_test.go (existing coverage)
  </read_first>
  <behavior>
    config_test.go:
    - TestWatcher_AtomicRenameTriggersReload (F9): start watcher on target file; write a NEW valid YAML to sibling temp file, Close() its handle (Windows), then os.Rename over the target; assert onChange fires with the new config within a generous timeout (poll a channel).
    - TestWatcher_IgnoresSiblingFiles (F9): create/write a different file in the same directory; onChange does NOT fire.
    - TestWatcher_MalformedYAMLKeepsPrevious (T-02-03 regression): overwrite target with invalid YAML; onChange not called; subsequent valid write fires.
    circuit_breaker_test.go:
    - TestCall_SingleHalfOpenTransition (F10): breaker in Open with lastFailureTime past timeout; N goroutines call Call concurrently with an operation that blocks on a gate then succeeds; with -race, assert no race and the breaker behaves consistently (ends Closed or HalfOpen per successThreshold; totalAttempts == N; no panic). The primary assertion is `go test -race` cleanliness on this path — the unlocked lastFailureTime read at line 65 races today.
  </behavior>
  <action>
    F9 — rewrite Watcher:
    - NewWatcher(path string, onChange func(*ConnectorsFileConfig), logger *zap.Logger) (*Watcher, error): nil logger → zap.NewNop(). Resolve absPath via filepath.Abs; fw.Add(filepath.Dir(absPath)) — watch the PARENT DIRECTORY, never the file inode. Store target base name on the Watcher.
    - Start: for each event, filter filepath.Base(event.Name) == target base (use filepath.Base, not string compare on full paths — fsnotify may report either slash style on Windows). React to Write|Create|Rename|Remove of the target: attempt LoadConnectorsConfig(w.path); on success call onChange; on error logger.Warn("connector watcher: reload failed", zap.String("path",...), zap.Error(err)) and keep previous config (preserves T-02-03). Rename/Remove may race the new file into place — a load error there is logged, and the subsequent Create/Write event of the replacement file triggers the successful reload. fsnotify Errors channel → logger.Warn. Remove BOTH fmt.Fprintf(os.Stderr, ...) calls; drop the now-unused "os" import if nothing else uses it.
    - Update every NewWatcher call site (grep `NewWatcher(` across the repo — expected: config_test.go only; agent wiring is still TODO).
    F10 — restructure Call:
    - Single cb.mu.Lock() at entry covering: read state; if Open → if time.Since(cb.lastFailureTime) > cb.timeout transition to HalfOpen (reset successes/failures) else Unlock + return open error. Unlock ONLY around `err := operation()` (and the atomic totalAttempts add). Re-lock for the existing result-handling block (unchanged logic). Net effect: no field guarded by mu is ever read outside it, and exactly one goroutine performs the Open->HalfOpen transition.
    Commits: fix(03-03): watcher survives atomic rename, logs via zap (F9) and fix(03-03): circuit breaker single-lock state decision (F10).
  </action>
  <verify>
    <automated>cd C:\Users\Drupad\argus-sdk && go test -race ./internal/connector/... ./internal/resilience/... -count=1 -timeout 120s 2>&1</automated>
  </verify>
  <done>Atomic-rename save fires onChange; no stderr writes in config.go; breaker Call has no unlocked guarded-field reads; both packages pass -race.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Secrets cache + 0600 temp (F11, F15); Kafka RequiredAcks pointer (F12); ActivityName=Other (F13); F17 deferral note</name>
  <files>internal/secrets/store.go, internal/secrets/env_fallback.go, internal/secrets/store_test.go, internal/connector/kafka/connector.go, internal/connector/kafka/connector_test.go, internal/ocsf/mapper.go, internal/ocsf/mapper_test.go</files>
  <read_first>
    - internal/secrets/store.go (Store struct, LoadSecrets, SaveSecrets:154-189)
    - internal/secrets/env_fallback.go (GetSecret:33-55)
    - internal/connector/kafka/connector.go (Config:28-49, New:77-97, Connect acks switch:116-123)
    - internal/ocsf/mapper.go (Map:270-331, Event literal at 287)
    - existing *_test.go in each package (conventions; kafka Config literal construction in tests)
  </read_first>
  <behavior>
    secrets:
    - TestGetSecret_UsesCache (F11): SetStore + SaveSecrets{k:v}; call GetSecret(k) twice; instrument by deleting the store FILE between the calls (Close handles first — Windows) — second call must STILL return v (served from cache, file not re-read). Then SaveSecrets{k:v2} (recreates file) → GetSecret returns v2 (invalidation works).
    - TestSaveSecrets_TempFilePerms (F15): on non-Windows assert os.Stat perms 0600 on the final file and that os.OpenFile mode is used; on Windows (this machine) the meaningful assertion is code-level — keep a test that SaveSecrets round-trips, and verify the 0600 OpenFile flag change via the contains-check in must_haves (Go perms are advisory on Windows).
    kafka:
    - TestNew_RequiredAcksNilDefaultsToLeader (F12): Config{RequiredAcks: nil} → effective acks 1 (expose effective value via white-box field check after New, or assert Connect builds LeaderAck path — white-box on c.cfg).
    - TestNew_RequiredAcksZeroMeansNoAck (F12): ptr(0) survives New un-defaulted; Connect's switch reaches the NoAck branch (white-box: assert c.cfg.RequiredAcks != nil && *c.cfg.RequiredAcks == 0 after New).
    ocsf:
    - TestMap_Activity99SetsActivityName (F13): Map a signal with Layer = signal.L8Agents (activity 99) → ev.ActivityID == 99 && ev.ActivityName == "Other". Also one non-99 layer → ActivityName stays "".
  </behavior>
  <action>
    F11: add to Store: `cacheMu sync.RWMutex; cache map[string]string; cacheValid bool`. Add method `(s *Store) lookup(key string) (string, bool, error)`: RLock fast path when cacheValid; else Lock, double-check, LoadSecrets into cache, set cacheValid. SaveSecrets: on successful write, under Lock set cache to a COPY of the saved map and cacheValid=true (or simply invalidate — either is acceptable; copying avoids the next decrypt). Never return the internal map to callers. env_fallback.GetSecret: replace the s.LoadSecrets() call with s.lookup(key) (ignore error → fall through to env, preserving current behavior).
    F15: store.go:156 — replace os.Create(tmpPath) with os.OpenFile(tmpPath, os.O_CREATE|os.O_WRONLY|os.O_TRUNC, 0600). Keep the final Chmod (harmless belt-and-braces).
    F12 (locked: pointer or sentinel — use POINTER for natural YAML compat): change Config field to `RequiredAcks *int` with doc: nil = default (1); 0 = no ack; 1 = leader; -1 = all ISR. New(): replace the `== 0` defaulting with `if cfg.RequiredAcks == nil { one := 1; cfg.RequiredAcks = &one }`. Connect(): switch on *c.cfg.RequiredAcks (the 0 case is now reachable). Grep the repo for `RequiredAcks` literal construction (tests, config decoding) and update; YAML/mapstructure decodes `required_acks: 0` into a non-nil pointer and an absent key into nil — document this on the field.
    F13: in Map() after `activityID := layerToActivityID(s.Layer)` (mapper.go:281), set ActivityName in the Event literal: add `ActivityName: activityName,` where `activityName := ""; if activityID == 99 { activityName = "Other" }`.
    F17 (deferred, locked decision 5): add a comment at mapper.go LoggedTime line: `// NOTE(F17): time.Now() makes Map non-deterministic for golden tests. Accepted/deferred 2026-06-10 review — clock injection is an API change touching all connectors.` Do NOT change behavior. Record the deferral in the SUMMARY.
    Commits: one per finding-group, citing IDs.
  </action>
  <verify>
    <automated>cd C:\Users\Drupad\argus-sdk && go test -race ./internal/secrets/... ./internal/connector/kafka/... ./internal/ocsf/... -count=1 -timeout 120s && go vet ./internal/... 2>&1</automated>
  </verify>
  <done>GetSecret serves from cache (file-deletion test proves no re-read); temp file opened 0600; RequiredAcks *int with reachable NoAck; activity 99 carries ActivityName "Other"; F17 comment present, behavior unchanged; all three packages pass -race.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML config file -> connector settings | Hot-reloadable, operator-writable; values reach the elastic /_bulk wire format (F4 sink) |
| Secrets store file on disk | AES-256-GCM encrypted; temp-file write window (F15) |
| Process memory | Raw APIKey retention (F16) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Tampering (injection) | Elastic action line from cfg.Index (F4) | mitigate | json.Marshal the action object — config values are data, never syntax; hostile-index test locks it |
| T-03-12 | Information Disclosure | Raw APIKey on Connector struct (F16) | mitigate | Zero cfg.APIKey after header derivation; Connect checks apiKeyHeader |
| T-03-13 | Denial of Service | Watcher silently dead after atomic rename (F9) | mitigate | Parent-dir watch + filename filter + Rename/Remove handling; failures logged via zap instead of lost on stderr |
| T-03-14 | Elevation/incorrect state | Circuit breaker double Open->HalfOpen (F10) | mitigate | Full decision under one mutex hold; -race test on concurrent Call |
| T-03-15 | Information Disclosure | Secrets temp file perms window (F15) | mitigate | O_CREATE|O_WRONLY|O_TRUNC with 0600 at creation |
| T-03-16 | Denial of Service | Per-call full-store decrypt (F11) | mitigate | RWMutex cache, invalidated/updated on SaveSecrets |
| T-03-17 | Repudiation | Unset-vs-NoAck conflation (F12) | mitigate | *int nil-means-default; explicit 0 reaches kgo.NoAck |
| T-03-18 | Tampering (schema rejection) | activity_id=99 without name (F13) | mitigate | ActivityName="Other" set in Map |
| T-03-19 | Repudiation | LoggedTime non-determinism (F17) | accept | Deferred per locked decision 5 — comment + SUMMARY note only |
| T-03-SC | Tampering | package installs | accept | No new dependencies; zap + fsnotify already in go.mod |
</threat_model>

<verification>
cd C:\Users\Drupad\argus-sdk && go test -race ./internal/connector/... -count=1 -timeout 120s
cd C:\Users\Drupad\argus-sdk && go test -race ./internal/resilience/... ./internal/secrets/... ./internal/ocsf/... -count=1 -timeout 120s
cd C:\Users\Drupad\argus-sdk && go vet ./internal/...
</verification>

<success_criteria>
- Elastic action line via json.Marshal; hostile index name test proves injection closed (SC-5 / F4); APIKey zeroed (F16)
- Watcher survives atomic-rename saves and logs via zap (SC-9 / F9)
- CircuitBreaker.Call single-lock decision, -race clean (SC-10 / F10)
- Secrets cache + 0600 temp file; Kafka RequiredAcks pointer; ActivityName for 99 (SC-11 / F11, F15, F12, F13)
- F17 documented-deferred, not fixed (locked decision 5)
- All touched packages pass go test -race; go vet clean; zero new dependencies
</success_criteria>

<output>
Create .planning/workstreams/ws-b-connector-layer/phases/03-review-remediation/03-03-SUMMARY.md when done
</output>
