---
plan: 03-04
phase: 3
wave: 3
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - internal/dryrun/dryrun.go
  - internal/dryrun/validator.go
  - internal/dryrun/dryrun_test.go
autonomous: true
requirements:
  - SC-8   # dryrun.Run uses per-signal mapper.Map with index alignment — mapper errors attribute the correct SignalID
  - SC-12  # go test -race ./... passes; go vet ./... clean (full module)

must_haves:
  truths:
    - "dryrun.Run builds events via a per-signal mapper.Map loop producing a parallel events slice with nil at every failed index — mapper.MapBatch is no longer called from dryrun (F8)"
    - "A batch with an invalid signal MID-BATCH attributes the mapper ValidationError to the CORRECT SignalID (the failing signal's, not a shifted neighbour's) (F8)"
    - "Report.ProtoValid and Report.OCSFValid are exact: OCSFValid counts only signals whose event mapped AND validated — no inflation from slice compaction (F8)"
    - "signals-ocsf.json output remains consistent with the per-index events slice (nil events skipped or serialized per existing writeOCSF behavior)"
    - "go test -race ./... -count=1 passes for the ENTIRE module (SC-12)"
    - "go vet ./... is clean for the entire module (SC-12)"
  artifacts:
    - path: "internal/dryrun/dryrun.go"
      provides: "Per-signal Map loop with index-aligned parallel events slice"
      contains: "mapper.Map("
    - path: "internal/dryrun/dryrun_test.go"
      provides: "Invalid-layer-mid-batch test asserting SignalID attribution and ProtoValid/OCSFValid counts"
      min_lines: 60
---

<objective>
Close finding F8 and run the phase-exit verification gate (SC-12).

F8 (high): dryrun.go:145 calls mapper.MapBatch, which COMPACTS both return slices (mapper.go:455-467 — failed signals are skipped in `events` and errors are appended without index info). dryrun then pairs events/errors with signals BY INDEX: error attribution uses the errs-slice index (wrong SignalID when any earlier signal failed) and the OCSFValid loop walks the compacted events slice against signal indexes (inflated/shifted counts). Locked fix: replace the MapBatch call with a per-signal `mapper.Map` loop building `events := make([]*ocsf.Event, len(signals))` — nil at failed indexes. validateOCSFBatch (validator.go:125) already skips nil events, so it needs no change unless verification reveals otherwise.

This plan is wave 2: it runs after 03-01/03-02/03-03 so its final task verifies the WHOLE remediated module with `go test -race ./...` and `go vet ./...` — the review's exit criterion (locked decision 6).

Constraints (from 03-CONTEXT.md, binding):
- No new dependencies. No proto / pkg/signal API changes. Do not modify mapper.MapBatch itself (other callers may rely on compaction semantics; the fix is at the dryrun call site).
- Windows: tests must Close() file handles before delete/rename; dryrun writes output files — use t.TempDir().
- Sequential execution on the main tree.

Purpose: dryrun is the operator's pre-flight validator; misattributed SignalIDs and inflated valid-counts make it lie about which signals are broken. The final task closes out the REQUEST CHANGES review verdict.
Output: index-aligned dryrun pipeline + green full-module race/vet gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/ws-b-connector-layer/ROADMAP.md
@.planning/workstreams/ws-b-connector-layer/phases/03-review-remediation/03-CONTEXT.md
@internal/dryrun/dryrun.go
@internal/dryrun/validator.go
</context>

<interfaces>
Current code the executor must know (verified against source 2026-06-10):

From internal/ocsf/mapper.go (DO NOT MODIFY in this plan):
  func (m *Mapper) Map(s signal.Signal) (*Event, error)
  func (m *Mapper) MapBatch(signals []signal.Signal) ([]*Event, []error)
    // COMPACTS: failed signals are skipped in events; errs collects fmt.Errorf("signal %s: %w", s.SignalID, err)
    // with NO index correspondence — this is the F8 root cause at the dryrun call site.
  Map returns error for unknown layers: layerToClassUID default → fmt.Errorf("unknown layer %d", l) — an invalid Layer value (e.g. 99) is the easiest mid-batch failure injector for tests.

From internal/dryrun/dryrun.go (the buggy block, lines 144-170):
  events, mapErrs := mapper.MapBatch(signals)
  for i, mapErr := range mapErrs { ... signalID(signals, i) ... }    // WRONG: i indexes the compacted errs slice, not signals
  ocsfErrs := validateOCSFBatch(events, signals)                      // events shorter than signals when any Map failed
  ...
  for i, ev := range events { if ev != nil && !ocsfFailSet[i] { report.OCSFValid++ } }   // shifted indexes

  Report fields used: Total, ProtoValid, OCSFValid, Errors []ValidationError, ProtoJSONFile, ProtoBinFile, OCSFJSONFile
  ValidationError { Index int; SignalID string; Stage string; Field string; Message string }
  Helpers: signalID(signals, i) string; indexSet(errs) map[int]bool; writeOCSF(path, events) (check how it handles nil entries)

From internal/dryrun/validator.go:
  func validateProto(signals []signal.Signal) []ValidationError      // line 93
  func validateOCSFBatch(events []*ocsf.Event, signals []signal.Signal) []ValidationError   // line 125 — already skips nil events per 03-CONTEXT
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing test — invalid layer mid-batch attribution (F8, RED)</name>
  <files>internal/dryrun/dryrun_test.go</files>
  <read_first>
    - internal/dryrun/dryrun.go (Run pipeline, Report/ValidationError shapes, LoadSignals input format)
    - internal/dryrun/dryrun_test.go (existing fixtures: how input files are built, OutputDir usage)
    - internal/dryrun/validator.go (validateOCSFBatch nil handling)
  </read_first>
  <behavior>
    TestRun_MapperErrorMidBatch_IndexAlignment (F8):
    - Build an input file (t.TempDir()) with 3 signals: sig-a (valid), sig-b (INVALID layer value so mapper.Map errors — e.g. Layer 99, while keeping proto-stage fields valid enough that the failure is unambiguously at the OCSF mapping stage), sig-c (valid). Use distinctive SignalIDs.
    - Run(cfg) with OutputDir in t.TempDir().
    - Assert exactly one Report.Errors entry with Stage "ocsf_schema"/Field "mapper"; its SignalID == "sig-b" and Index == 1 (against current code this attributes index 0 / sig-a — the compaction bug — so this test FAILS before the fix).
    - Assert report.Total == 3, report.ProtoValid == 3 (or per proto-stage reality of the fixture), and report.OCSFValid == 2 — sig-a and sig-c only, no inflation.
    - Assert the OCSF output file exists and contains events for sig-a and sig-c (Metadata.UID), not for sig-b.
    - Close all created file handles before the test ends (Windows).
  </behavior>
  <action>
    Write the test exactly as specified; run it and confirm it fails on SignalID attribution and/or OCSFValid count against the current MapBatch-based pipeline. Commit: test(03-04): failing index-alignment test for dryrun mapper errors (F8).
  </action>
  <verify>
    <automated>cd C:\Users\Drupad\argus-sdk && go test -race ./internal/dryrun/... -count=1 -timeout 120s 2>&1</automated>
  </verify>
  <done>New test exists and fails for the documented F8 reason; all pre-existing dryrun tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Per-signal Map loop with parallel events slice (F8, GREEN)</name>
  <files>internal/dryrun/dryrun.go, internal/dryrun/validator.go</files>
  <read_first>
    - internal/dryrun/dryrun_test.go (Task 1 contract)
    - internal/dryrun/dryrun.go lines 134-183 (the block being replaced; writeOCSF call)
  </read_first>
  <action>
    Replace the MapBatch block (dryrun.go:144-156) with a per-signal loop:
      events := make([]*ocsf.Event, len(signals))
      for i, s := range signals {
          map each via mapper.Map(s); on error append ValidationError{Index: i, SignalID: signals[i].SignalID, Stage: "ocsf_schema", Field: "mapper", Message: err.Error()} and leave events[i] nil; on success events[i] = ev.
      }
    Downstream: validateOCSFBatch(events, signals) now receives a len-aligned slice (it already skips nils — confirm; touch validator.go ONLY if a nil-handling or length assumption breaks, and keep any change minimal). The mapErr-failure marking loop folds into the Map loop (track failed indexes directly). The OCSFValid count loop is now correct as `for i, ev := range events { if ev != nil && !ocsfFailSet[i] { report.OCSFValid++ } }` since indexes align. Verify writeOCSF tolerates nil entries (it predates this change with possibly-compacted input — if it serializes nils as JSON null or panics, make it skip nil events; preserve existing output shape for all-valid batches).
    Do NOT modify internal/ocsf/mapper.go. Commit: fix(03-04): dryrun per-signal Map with index-aligned events (F8).
  </action>
  <verify>
    <automated>cd C:\Users\Drupad\argus-sdk && go test -race ./internal/dryrun/... -count=1 -timeout 120s && go vet ./internal/dryrun/... 2>&1</automated>
  </verify>
  <done>Task 1 test passes; mapper.MapBatch no longer referenced in dryrun.go; all dryrun tests green under -race.</done>
</task>

<task type="auto">
  <name>Task 3: Phase-exit gate — full-module race + vet (SC-12)</name>
  <files>(none — verification only; fixes limited to phase scope if the gate fails)</files>
  <action>
    Run the locked phase-exit verification (03-CONTEXT locked decision 6) across the ENTIRE module:
      1. cd C:\Users\Drupad\argus-sdk && go test -race ./... -count=1 -timeout 600s
      2. cd C:\Users\Drupad\argus-sdk && go vet ./...
      3. cd C:\Users\Drupad\argus-sdk && go build ./...
    If any failure traces to code touched in plans 03-01..03-04, fix it within those files (cite the finding ID in the commit). If a failure is OUTSIDE phase scope (pre-existing, unrelated to F1-F17), do NOT fix it — record it verbatim in the SUMMARY as a new finding for triage. Record in the SUMMARY: the full command outputs (pass/fail per package), confirmation that all 16 fixed findings have closing commits, and the F17 documented-deferral.
  </action>
  <verify>
    <automated>cd C:\Users\Drupad\argus-sdk && go test -race ./... -count=1 -timeout 600s && go vet ./... 2>&1</automated>
  </verify>
  <done>go test -race ./... passes for every package in the module; go vet ./... reports nothing; results recorded in SUMMARY (SC-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator input file -> dryrun pipeline | Untrusted signal fixtures parsed and validated |
| dryrun output files | Written to operator-chosen OutputDir (0750) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-20 | Repudiation | Misattributed SignalID in validation report (F8) | mitigate | Index-aligned per-signal Map loop; mid-batch failure test locks correct attribution |
| T-03-21 | Tampering (false confidence) | Inflated OCSFValid counts (F8) | mitigate | Counts derived from the parallel nil-padded slice; exact-count assertions in test |
| T-03-22 | Denial of Service | Module-wide race conditions | mitigate | Full-module `go test -race ./...` gate (SC-12, locked decision 6) |
| T-03-SC | Tampering | package installs | accept | No new dependencies (locked constraint) |
</threat_model>

<verification>
cd C:\Users\Drupad\argus-sdk && go test -race ./internal/dryrun/... -count=1 -timeout 120s
cd C:\Users\Drupad\argus-sdk && go test -race ./... -count=1 -timeout 600s
cd C:\Users\Drupad\argus-sdk && go vet ./...
</verification>

<success_criteria>
- dryrun uses per-signal mapper.Map with a nil-padded parallel events slice; MapBatch removed from dryrun (SC-8 / F8)
- Invalid-layer-mid-batch test passes: correct SignalID attribution, exact ProtoValid/OCSFValid counts (SC-8 / F8)
- go test -race ./... -count=1 passes module-wide; go vet ./... clean (SC-12)
- SUMMARY records the verification output, all finding-closure commits, and the F17 deferral
</success_criteria>

<output>
Create .planning/workstreams/ws-b-connector-layer/phases/03-review-remediation/03-04-SUMMARY.md when done
</output>
