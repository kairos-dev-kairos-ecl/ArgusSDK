---
phase: 04-agent-wiring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/connector/syslog/connector.go
  - internal/connector/syslog/connector_test.go
autonomous: true
requirements: [SC-7]
must_haves:
  truths:
    - "syslog Send() writes one CEF-formatted message per signal to a TCP listener"
    - "Send() uses a TLS 1.3 connection (via connector.NewTLSConfig) when Transport is tls"
    - "A delivery failure (write error / unreachable server) returns a non-nil error and failed ack"
    - "CEF header carries Argus vendor/product and per-signal fields (severity, layer, category)"
  artifacts:
    - path: "internal/connector/syslog/connector.go"
      provides: "Connect dials TCP/TLS; Send emits CEF per signal — no longer a scaffold"
      contains: "CEF:0"
    - path: "internal/connector/syslog/connector_test.go"
      provides: "unit tests against a local net.Listen TCP server and a TLS listener"
  key_links:
    - from: "internal/connector/syslog/connector.go"
      to: "connector.NewTLSConfig"
      via: "tls.Client / tls.Dial when Transport==TransportTLS"
      pattern: "NewTLSConfig"
---

<objective>
Implement the syslog connector Send()/Connect() bodies: CEF (Common Event Format) over TCP, with
TLS 1.3 when Transport is "tls". Replace the scaffold that currently returns a fake "delivered"
ack without writing anything.

Purpose: SC-7 — syslog must be a real, hardened fourth connector (locked decision 4): stdlib net +
crypto/tls only, NO new dependency, TLS via connector.NewTLSConfig, honoring the Phase 3 delivery
contract (failed delivery ⇒ non-nil error + failed ack).

Output: implemented internal/connector/syslog/connector.go + Docker-free unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/ws-b-connector-layer/phases/04-agent-wiring/04-CONTEXT.md

<interfaces>
From internal/connector/connector.go:
- type SignalBatch struct { BatchID, InstanceID, GroupID string; Signals []signal.Signal; ReceivedAt time.Time; UseOCSF bool }
- type DeliveryAck struct { BatchID, Status, RemoteID, Error string; Timestamp time.Time }
- func NewTLSConfig(TLSClientConfig{CACert,CertFile,KeyFile string}) (*tls.Config, error) // MinVersion TLS 1.3

From pkg/signal/signal.go:
- type Signal struct { SignalID, TraceID, SpanID string; Layer Layer; Category string; Severity Severity; AppID, Env string; Timestamp time.Time; DurationMS float32; ContextJSON []byte; ... }
- Severity values: SeverityInfo=1, SeverityLow=2, SeverityMedium=3, SeverityHigh=4, SeverityCritical=5
- Layer values: L1Hardware..L10Application, LDecision (int32-backed)

From internal/connector/syslog/connector.go (current scaffold to implement):
- Config{ Server string; Transport Transport("udp"|"tcp"|"tls"); Facility int; AppName string; TLS TLSConfig{CACert,CertFile,KeyFile} }
- struct Connector{ cfg Config; conn net.Conn }
- New(cfg) sets defaults; Connect/Send/Health/Close exist as scaffolds.
- CEF template (from package doc): CEF:0|Argus|SDK|1.0|<signal_id>|<category>|<cef_severity>|layer=<layer> app_id=<app_id> trace_id=<trace_id>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Connect dials TCP/TLS; CEF formatter</name>
  <files>internal/connector/syslog/connector.go, internal/connector/syslog/connector_test.go</files>
  <read_first>internal/connector/syslog/connector.go, internal/connector/tls.go, pkg/signal/signal.go</read_first>
  <behavior>
    - Connect with Transport "tcp" dials the configured Server and stores a net.Conn.
    - Connect with Transport "tls" dials via tls.Client using connector.NewTLSConfig (TLS 1.3).
    - Connect with empty Server returns an error (preserve existing guard).
    - A pure formatting helper buildCEF(signal, appName) returns a string starting with "CEF:0|Argus|SDK|"
      and containing the signal Category, a mapped CEF severity integer (Info..Critical → 0..10 band),
      and extension key=value pairs for layer, app_id, trace_id.
  </behavior>
  <action>
    Implement Connect: switch on c.cfg.Transport. For TransportTCP use net.DialTimeout("tcp",
    c.cfg.Server, timeout). For TransportTLS build *tls.Config via connector.NewTLSConfig(
    connector.TLSClientConfig{CACert,CertFile,KeyFile from c.cfg.TLS}) then tls.DialWithDialer (or
    net.Dial + tls.Client) — NEVER construct tls.Config directly (locked decision 4 / TLS 1.3). For
    TransportUDP keep net.Dial("udp", ...). Store the connection on c.conn. Add an unexported
    buildCEF(s signal.Signal, appName string) string that renders the CEF prefix
    `CEF:0|Argus|SDK|1.0|<SignalID>|<Category>|<cefSeverity>|` followed by space-separated extensions
    `layer=<int> app_id=<AppID> trace_id=<TraceID>`. Map signal Severity (1..5) to a CEF severity
    integer (0-10): Info→1, Low→3, Medium→5, High→7, Critical→9 (document the mapping). Use stdlib
    fmt only; no new dependency. Write the formatter + Connect tests against a local net.Listen("tcp")
    server before implementing.
  </action>
  <verify>
    <automated>cd C:\Users\Drupad\argus-sdk && go test ./internal/connector/syslog/ -run "TestConnect|TestBuildCEF" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Tests for Connect (tcp) and buildCEF pass.
    - `grep -n "NewTLSConfig" internal/connector/syslog/connector.go` confirms TLS path uses the helper.
    - `grep -n "tls.Config{" internal/connector/syslog/connector.go` returns no direct struct literal.
  </acceptance_criteria>
  <done>Connect establishes TCP/TLS connections via NewTLSConfig and buildCEF produces a valid CEF line.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Send writes CEF per signal with delivery contract</name>
  <files>internal/connector/syslog/connector.go, internal/connector/syslog/connector_test.go</files>
  <read_first>internal/connector/syslog/connector.go, internal/connector/connector.go</read_first>
  <behavior>
    - Send over a connected TCP listener writes one newline-terminated CEF message per signal;
      the test listener receives N lines for an N-signal batch, each starting with "CEF:0|Argus|SDK|".
    - Send when c.conn is nil (Connect not called / failed) returns a non-nil error AND a
      DeliveryAck{Status:"failed"} (Phase 3 delivery contract, locked decision 9).
    - A write error to a closed connection returns a non-nil error and failed ack (no silent success).
    - On full success Send returns DeliveryAck{Status:"delivered"} with BatchID echoed.
  </behavior>
  <action>
    Implement Send: guard c.conn == nil → return failed ack + error. Iterate batch.Signals, call
    buildCEF, append '\n', write to c.conn. On the first write error abort and return a failed ack +
    error referencing the failing signal index (sequential abort-on-first-failure mirrors the
    chunking contract in elastic/splunk, locked decision 9). On full success return delivered ack
    with batch.BatchID and time.Now(). Do NOT translate to OCSF (syslog emits CEF directly from the
    signal; UseOCSF is irrelevant here). Write Send tests against a local net.Listen TCP server that
    reads and counts received lines, plus a failure test (closed connection) before implementing.
  </action>
  <verify>
    <automated>cd C:\Users\Drupad\argus-sdk && go test ./internal/connector/syslog/ -count=1 && go vet ./internal/connector/syslog/...</automated>
  </verify>
  <acceptance_criteria>
    - Full syslog package test suite passes Docker-free.
    - The happy-path test asserts the listener received exactly len(batch.Signals) CEF lines.
    - The failure test asserts Send returns a non-nil error and ack.Status == "failed".
  </acceptance_criteria>
  <done>syslog Send delivers CEF over TCP/TLS and obeys the failed⇒error delivery contract; no longer a scaffold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → syslog server | Signal data crosses the network to an external SIEM over TCP/TLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Information disclosure | TLS transport | mitigate | TLS 1.3 enforced via connector.NewTLSConfig; no direct tls.Config; no InsecureSkipVerify |
| T-04-05 | Tampering | CEF injection | mitigate | CEF extension values come from typed signal fields; newline/pipe in field values escaped or constrained so a signal cannot inject extra CEF records |
| T-04-06 | Repudiation | silent drop | mitigate | Failed write returns non-nil error + failed ack (no fake "delivered"), preserving Phase 3 contract |
</threat_model>

<verification>
cd C:\Users\Drupad\argus-sdk && go build ./... && go vet ./... && go test ./internal/connector/syslog/ -count=1 -timeout 120s
</verification>

<success_criteria>
- SC-7: syslog Send() delivers CEF-formatted events over TCP/TLS (TLS 1.3), unit-tested with a
  local listener; failed delivery returns a non-nil error; no new dependency added.
</success_criteria>

<output>
Create .planning/workstreams/ws-b-connector-layer/phases/04-agent-wiring/04-02-SUMMARY.md when done
</output>
