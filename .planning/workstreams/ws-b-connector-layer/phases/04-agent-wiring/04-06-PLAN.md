---
phase: 04-agent-wiring
plan: 06
type: execute
wave: 3
depends_on: [04-01, 04-02, 04-03, 04-04, 04-05]
files_modified:
  - internal/connector/kafka/connector_integration_test.go
  - internal/connector/elastic/connector_integration_test.go
  - internal/connector/splunk/connector_integration_test.go
  - cmd/argus-agent/smoke_test.go
  - .github/workflows/ci.yml
  - go.mod
  - go.sum
autonomous: true
requirements: [SC-9, SC-10, SC-11, SC-12]
must_haves:
  truths:
    - "Kafka and Elasticsearch integration tests deliver via testcontainers-go and pass when Docker is present"
    - "Each integration test calls t.Skip cleanly when Docker is unavailable"
    - "A Splunk HEC integration or documented gated smoke test exists behind the integration tag"
    - "A CI workflow runs go build, go vet, CGO-enabled go test -race, and the integration job"
    - "cmd/argus-agent has an end-to-end smoke that boots config, ingests, and delivers with graceful drain"
  artifacts:
    - path: "internal/connector/kafka/connector_integration_test.go"
      provides: "//go:build integration Kafka delivery test via testcontainers-go"
      contains: "//go:build integration"
    - path: ".github/workflows/ci.yml"
      provides: "unit+vet+race+integration jobs; race job CGO_ENABLED=1 on ubuntu-latest"
      contains: "CGO_ENABLED"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "go test -race ./..."
      via: "dedicated race job with CGO_ENABLED=1"
      pattern: "race"
    - from: "internal/connector/kafka/connector_integration_test.go"
      to: "testcontainers-go"
      via: "container start + skip-on-no-docker guard"
      pattern: "testcontainers"
---

<objective>
Add real-infrastructure integration tests (Kafka + Elasticsearch via testcontainers-go, Splunk HEC
via container-if-feasible-else-documented-gated-smoke), an end-to-end cmd/argus-agent smoke test,
and the GitHub Actions CI workflow that runs build, vet, the CGO-enabled `-race` gate, and the
integration job. All integration tests are gated behind `//go:build integration` and t.Skip cleanly
when Docker is absent so the default unit suite stays fast and Docker-free.

Purpose: SC-9 (Kafka+Elastic testcontainers), SC-10 (Splunk HEC integration/smoke), SC-11 (CI with
CGO -race gate), SC-12 (cmd/argus-agent end-to-end smoke + graceful drain — the integration half).

This is the final wave: it depends on the implemented connectors (04-01..04-03), collectors
(04-04), and agent wiring (04-05). testcontainers-go is the ONLY allowed new dependency
(test-only). Run AFTER all prior plans are merged on the main tree.

Output: integration test files, cmd smoke test, .github/workflows/ci.yml, updated go.mod/go.sum.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/ws-b-connector-layer/phases/04-agent-wiring/04-CONTEXT.md
@.planning/workstreams/ws-b-connector-layer/phases/04-agent-wiring/04-05-SUMMARY.md
@Makefile

<interfaces>
From internal/connector/kafka/connector.go:
- func New(Config{Brokers,Topic,TLS,SASL,...}) *Connector ; (c).Connect(ctx); (c).Send(ctx,*connector.SignalBatch); (c).Health(ctx); (c).Close()
From internal/connector/elastic/connector.go:
- func New(Config{Endpoint,APIKey,Index,TLS,MaxBatchDocs}) *Connector ; same Connector method set
From internal/connector/splunk/connector.go:
- func New(Config{Endpoint,Token,Index,...}) *Connector ; (c).Connect/Send/Health/Close
From internal/connector/connector.go:
- type SignalBatch struct { BatchID, InstanceID, GroupID string; Signals []signal.Signal; ReceivedAt time.Time; UseOCSF bool }
From internal/agent (built in 04-05):
- agent.New(*agent.Config, *zap.Logger) (*agent.Agent, error); (a).Run() — and the in-process wiring used by agent_test.go

Build/verify constraints (from 04-CONTEXT.md test environment notes):
- Dev machine has NO C compiler → -race CANNOT run locally. The -race gate is asserted by the CI
  workflow file existing and being correct, NOT by running -race here.
- Dev machine may lack Docker → integration tests MUST t.Skip cleanly when Docker is unavailable.
- Local verification uses: `go test ./... -count=1 -timeout 300s` (no -race), `go vet ./...`, `go build ./...`.

testcontainers-go: github.com/testcontainers/testcontainers-go (+ modules/kafka, modules/elasticsearch).
ONLY test-only new dependency permitted (locked decision 6 / scope fence).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Kafka + Elasticsearch integration tests (testcontainers-go)</name>
  <files>internal/connector/kafka/connector_integration_test.go, internal/connector/elastic/connector_integration_test.go, go.mod, go.sum</files>
  <read_first>internal/connector/kafka/connector.go, internal/connector/elastic/connector.go, internal/connector/connector.go, Makefile</read_first>
  <acceptance_criteria>
    - Both files begin with `//go:build integration` (first line) so the default unit suite ignores them.
    - Each test detects Docker absence and calls t.Skip with a clear message instead of failing
      (check via a testcontainers availability probe or a docker-ping helper; on probe error → t.Skip).
    - Kafka test: start a Kafka/Redpanda container, Connect a kafka.Connector to the mapped broker,
      Send a multi-signal connector.SignalBatch (UseOCSF=true), assert DeliveryAck.Status=="delivered"
      and (where feasible) consume the produced records to confirm count and headers.
    - Elastic test: start an Elasticsearch container, Connect an elastic.Connector with API key/TLS as
      the container exposes, Send a batch large enough to exercise multi-chunk (> MaxBatchDocs),
      assert delivered and that documents are indexed (GET _count or _search).
    - A real error case is covered (e.g. wrong topic ACL / wrong index perms OR a closed container)
      asserting Send returns a non-nil error + failed ack (preserves Phase 3 delivery contract).
    - go.mod lists github.com/testcontainers/testcontainers-go (and the kafka/elasticsearch modules)
      as the ONLY new modules; `go build ./...` (no tags) still succeeds and pulls no new runtime dep
      into non-test packages.
    - `cd C:\Users\Drupad\argus-sdk && go vet -tags=integration ./internal/connector/kafka/ ./internal/connector/elastic/` passes (compiles the tagged files).
  </acceptance_criteria>
  <action>
    Create the two `//go:build integration` test files. Use testcontainers-go modules (kafka,
    elasticsearch) to spin up containers, read the mapped endpoint/broker, construct the connector
    via its New + Connect, and Send realistic connector.SignalBatch values (build signals with
    pkg/signal.Signal fields populated so OCSF mapping succeeds). Add a small dockerAvailable()
    helper per file (or a shared testhelper) that probes the Docker daemon; when unavailable call
    t.Skip("docker unavailable"). Assert delivered acks and verify side effects (consume Kafka
    records; query Elastic _count). Add the multi-chunk Elastic case (> MaxBatchDocs) to exercise
    F7 sequential chunking, and one failure case asserting failed⇒error. Run `go mod tidy` to add
    testcontainers-go to go.mod/go.sum; confirm no new module leaks into runtime packages (it must
    appear only under test files). Do NOT modify any connector production file.
  </action>
  <verify>
    <automated>cd C:\Users\Drupad\argus-sdk && go build ./... && go vet ./... && go vet -tags=integration ./internal/connector/kafka/ ./internal/connector/elastic/ && go test ./... -count=1 -timeout 300s</automated>
  </verify>
  <done>Kafka + Elastic integration tests exist behind the integration tag, skip without Docker, and pass with Docker — SC-9.</done>
</task>

<task type="auto">
  <name>Task 2: Splunk HEC integration / gated smoke test</name>
  <files>internal/connector/splunk/connector_integration_test.go</files>
  <read_first>internal/connector/splunk/connector.go, internal/connector/connector.go</read_first>
  <acceptance_criteria>
    - File begins with `//go:build integration`.
    - Either: (a) a testcontainers-based HEC test that starts a Splunk container, configures a HEC
      token, Connects a splunk.Connector, Sends a batch and asserts delivered + indexed; OR
      (b) a documented gated smoke test that reads SPLUNK_HEC_ENDPOINT + SPLUNK_HEC_TOKEN from env
      and t.Skip("set SPLUNK_HEC_ENDPOINT/TOKEN to run") when unset (SC-10 fallback). The chosen
      approach is documented in the file's package/test comment with the rationale.
    - When the Splunk container image is not pullable / Docker absent / env unset, the test t.Skips
      cleanly — never a hard failure in the default or tagged suite without infra.
    - A failure path asserts Send returns non-nil error + failed ack on a bad token / non-2xx HEC response.
  </acceptance_criteria>
  <action>
    Create the `//go:build integration` Splunk test. Prefer the testcontainers HEC path if a usable
    Splunk image is available in CI; otherwise implement the documented env-gated smoke test variant
    (SPLUNK_HEC_ENDPOINT / SPLUNK_HEC_TOKEN) per locked decision 6 / SC-10 fallback. Either way,
    Send a connector.SignalBatch and assert delivered; add the bad-token failure case. Reuse the
    dockerAvailable()/env-presence guards to t.Skip cleanly. No production-file changes.
  </action>
  <verify>
    <automated>cd C:\Users\Drupad\argus-sdk && go vet -tags=integration ./internal/connector/splunk/ && go test ./... -count=1 -timeout 120s</automated>
  </verify>
  <done>Splunk HEC integration or documented gated smoke test exists behind the tag and skips cleanly — SC-10.</done>
</task>

<task type="auto">
  <name>Task 3: CI workflow + cmd/argus-agent end-to-end smoke</name>
  <files>.github/workflows/ci.yml, cmd/argus-agent/smoke_test.go</files>
  <read_first>cmd/argus-agent/main.go, internal/agent/agent.go, Makefile, .planning/workstreams/ws-b-connector-layer/phases/04-agent-wiring/04-05-SUMMARY.md</read_first>
  <acceptance_criteria>
    - .github/workflows/ci.yml defines three jobs on ubuntu-latest:
      (a) build+vet: `go build ./...` and `go vet ./...`;
      (b) race: `go test -race ./...` with `CGO_ENABLED=1` (the authoritative race gate, SC-11 /
          locked decision 7) — env block contains `CGO_ENABLED: 1`;
      (c) integration: runs `make test-int` (i.e. `go test ./... -tags=integration`) with Docker
          available (service containers or the default ubuntu Docker daemon).
    - The workflow uses actions/setup-go pinned to the go.mod toolchain (go 1.26.x) and triggers on
      push + pull_request.
    - cmd/argus-agent/smoke_test.go is a Docker-free in-process smoke (NOT behind the integration
      tag): it builds an agent.Config in memory with a fake/loopback output, boots the agent enough
      to ingest one signal, asserts delivery, and triggers graceful shutdown asserting the buffer
      drains without panic. (Reuse the in-process seam established by agent_test.go in 04-05; if the
      cmd layer cannot reach internal seams, place the smoke in internal/agent and document that the
      cmd boot path is covered by the build+vet job + a documented manual run in the SUMMARY.)
    - The CI YAML is syntactically valid (parse-check) and the race job is clearly the gate.
  </acceptance_criteria>
  <action>
    Create .github/workflows/ci.yml with the three jobs above; pin setup-go to the go.mod version,
    set `CGO_ENABLED: 1` only on the race job (locked decision 7 — the dev box has no C compiler, so
    CI is the authoritative race gate). The integration job runs `make test-int`. Add
    cmd/argus-agent/smoke_test.go (Docker-free, no integration tag) exercising the end-to-end
    ingest→deliver→graceful-drain path via the in-process seam from 04-05; assert one signal is
    delivered to a fake connector and stop() drains cleanly. If the smoke genuinely needs internal
    test seams not visible from package main, instead add it under internal/agent as
    e2e_smoke_test.go and note in the SUMMARY that cmd boot is covered by build+vet plus a documented
    manual run (`go run ./cmd/argus-agent --config sample.yaml`). Document the manual smoke command
    and the local `-race` limitation (needs gcc/mingw) in the SUMMARY. Do NOT change main.go behavior.
  </action>
  <verify>
    <automated>cd C:\Users\Drupad\argus-sdk && go build ./... && go vet ./... && go test ./cmd/... ./internal/agent/... -count=1 -timeout 180s</automated>
  </verify>
  <done>CI workflow (build/vet/race/integration) exists with the CGO race gate and an end-to-end smoke proves ingest→deliver→drain — SC-11 and SC-12.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → container registry | Test infra pulls Kafka/Elastic/Splunk images |
| integration tests → real brokers | Test signals + credentials cross to ephemeral containers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-19 | Tampering | new test dependency | mitigate | Only testcontainers-go (+ its modules) added, test-only; verified absent from runtime packages via `go build ./...` and go.mod review |
| T-04-20 | Denial of service | CI race gate | mitigate | CGO-enabled `-race` job on Linux is the authoritative gate; flaky/racy code fails CI before merge |
| T-04-21 | Repudiation | integration delivery contract | mitigate | Integration tests assert failed⇒error on bad creds/responses, preventing regression of Phase 3 F6/F7 |
| T-04-SC | Tampering | testcontainers install | mitigate | Single known test-only module; package legitimacy verifiable via pkg.go.dev/github.com/testcontainers/testcontainers-go (well-known project) |
</threat_model>

<verification>
cd C:\Users\Drupad\argus-sdk && go build ./... && go vet ./... && go vet -tags=integration ./internal/connector/... && go test ./... -count=1 -timeout 300s
# -race is NOT run locally (no C compiler); it is asserted via the presence and correctness of .github/workflows/ci.yml (race job, CGO_ENABLED=1).
</verification>

<success_criteria>
- SC-9: Kafka + Elasticsearch integration tests deliver via testcontainers-go and pass (TLS/auth,
  real error responses, multi-chunk), skipping cleanly without Docker.
- SC-10: Splunk HEC integration test or documented gated smoke test runs behind the integration tag.
- SC-11: CI workflow runs go build, go vet, and CGO-enabled go test -race on Linux, plus an
  integration job running make test-int.
- SC-12 (integration half): cmd/argus-agent (or the documented in-process equivalent) boots, ingests
  a signal, delivers it end-to-end, and drains the buffer on graceful shutdown.
- No new runtime dependency: testcontainers-go is test-only.
</success_criteria>

<output>
Create .planning/workstreams/ws-b-connector-layer/phases/04-agent-wiring/04-06-SUMMARY.md when done
</output>
