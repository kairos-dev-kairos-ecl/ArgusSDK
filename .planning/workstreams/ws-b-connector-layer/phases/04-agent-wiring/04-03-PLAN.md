---
phase: 04-agent-wiring
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/connector/argusxdr/connector.go
  - internal/connector/argusxdr/connector_test.go
autonomous: true
requirements: [SC-8]
must_haves:
  truths:
    - "argusxdr Connect dials the gRPC endpoint with TLS 1.3 and per-RPC API-key credentials"
    - "Send marshals batch signals to sdkv1.SignalBatch (BatchId/AppId/Env/Signals/SdkVersion only) and calls SDKIngestService.IngestBatch"
    - "InstanceID and GroupID travel in per-RPC gRPC metadata, NOT in the proto body (proto SignalBatch has no such fields)"
    - "Proto AppId/Env are sourced from the connector.SignalBatch AppID/Env fields, never from Signals[0]"
    - "A gRPC error response returns a non-nil error and failed ack (delivery contract)"
    - "UseOCSF is never honored — argusxdr always sends the proto wire format"
  artifacts:
    - path: "internal/connector/argusxdr/connector.go"
      provides: "gRPC IngestBatch client — no longer a scaffold"
      contains: "IngestBatch"
    - path: "internal/connector/argusxdr/connector_test.go"
      provides: "unit tests against an in-process gRPC server (bufconn or loopback)"
  key_links:
    - from: "internal/connector/argusxdr/connector.go"
      to: "sdkv1.SDKIngestServiceClient.IngestBatch"
      via: "client stub created in Connect, called in Send"
      pattern: "IngestBatch"
    - from: "internal/connector/argusxdr/connector.go"
      to: "google.golang.org/grpc/metadata"
      via: "metadata.NewOutgoingContext carries x-argus-instance-id and x-argus-group-id per RPC"
      pattern: "metadata\\.(NewOutgoingContext|Pairs)"
---

<objective>
Implement the argusxdr connector Send()/Connect() bodies: a gRPC client that marshals signals to
sdkv1.SignalBatch and calls SDKIngestService.IngestBatch over TLS with API-key per-RPC creds.
Replace the scaffold that returns a fake "delivered" ack.

Purpose: SC-8 — argusxdr is the Mode 1 connector; it must speak the generated proto contract (NO
OCSF, locked decision 5), gRPC is already a dependency (NO new dependency), TLS via
connector.NewTLSConfig, honoring the Phase 3 failed⇒error contract.

Output: implemented internal/connector/argusxdr/connector.go + Docker-free in-process gRPC tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/ws-b-connector-layer/phases/04-agent-wiring/04-CONTEXT.md

<interfaces>
From internal/connector/connector.go:
- type SignalBatch struct { BatchID, InstanceID, GroupID string; Signals []signal.Signal; ReceivedAt time.Time; UseOCSF bool; AppID, Env string }
  // NOTE: AppID and Env are added to this internal Go struct by plan 04-05 (the ingest loop populates them
  // from the collector's signal.Batch.AppID/Env). They are NOT part of the proto contract or pkg/signal.
  // Phase 3 delivery connectors (kafka/splunk/elastic) ignore these two fields — no contract break.
- type DeliveryAck struct { BatchID, Status, RemoteID, Error string; Timestamp time.Time }
- func NewTLSConfig(TLSClientConfig{CACert,CertFile,KeyFile string}) (*tls.Config, error)

From pkg/signal/signal.go (DO NOT MODIFY — scope fence):
- type Batch struct { AppID, Env string; Signals []Signal }
- func (b Batch) ToProto(batchID, sdkVersion string) *sdkv1.SignalBatch
  // ToProto sources proto AppId/Env from b.AppID/b.Env (the Batch struct's own fields), NOT from Signals[0].
  // signal.FromProto (signal.go:95) intentionally does NOT set AppID/Env/SDKVersion on per-signal Signal
  // structs ("set from SignalBatch by caller"), so Signals[i].AppID is typically EMPTY — never read it.

From gen/go/sdk/v1/ingest.pb.go (proto contract — DO NOT MODIFY):
- type SignalBatch struct { BatchId, AppId, Env string; Signals []*SDKSignal; SdkVersion string }
  // These are the ONLY five proto fields. There is NO InstanceId and NO GroupId in the proto SignalBatch.
  // InstanceID/GroupID MUST be transmitted as gRPC per-RPC metadata, never embedded in the proto body.

From gen/go/sdk/v1/ingest_grpc.pb.go:
- type SDKIngestServiceClient interface { IngestBatch(ctx, *SignalBatch, ...grpc.CallOption) (*BatchAck, error) }
- func NewSDKIngestServiceClient(cc grpc.ClientConnInterface) SDKIngestServiceClient
- type SDKIngestServiceServer interface { IngestBatch(ctx, *SignalBatch) (*BatchAck, error); mustEmbedUnimplementedSDKIngestServiceServer() }
- UnimplementedSDKIngestServiceServer (embed by value); RegisterSDKIngestServiceServer(s, srv)
- BatchAck{ BatchId string; AcceptedCount, RejectedCount int32; Rejections []*RejectionDetail }

From internal/connector/argusxdr/connector.go (current scaffold to implement):
- Config{ Endpoint string; TLS TLSConfig{Enabled,CACert,CertFile,KeyFile}; Auth AuthConfig{GroupID,InstanceID,Credential}; MaxBatchSize int }
- struct Connector{ cfg Config } — add the gRPC conn + client stub fields

gRPC libs already available (NO new dependency): google.golang.org/grpc, .../credentials,
.../credentials/insecure (loopback test only), google.golang.org/grpc/metadata (per-RPC instance/group
headers), google.golang.org/grpc/test/bufconn (test).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Connect — gRPC dial with TLS + per-RPC API key</name>
  <files>internal/connector/argusxdr/connector.go, internal/connector/argusxdr/connector_test.go</files>
  <read_first>internal/connector/argusxdr/connector.go, internal/connector/tls.go, gen/go/sdk/v1/ingest_grpc.pb.go</read_first>
  <behavior>
    - Connect with empty Endpoint returns an error.
    - Connect with TLS.Enabled builds transport credentials from connector.NewTLSConfig (TLS 1.3)
      via credentials.NewTLS and dials with grpc.NewClient.
    - Connect installs a per-RPC credential carrying the Credential as an API key (metadata header
      e.g. "x-argus-api-key") via grpc.WithPerRPCCredentials.
    - After Connect the connector holds a non-nil SDKIngestServiceClient stub.
    - A NewWithClient(cfg, client SDKIngestServiceClient) test constructor injects a stub so unit
      tests bypass real dialing.
  </behavior>
  <action>
    Add fields to the Connector struct: a *grpc.ClientConn and an sdkv1.SDKIngestServiceClient.
    Implement Connect: guard empty Endpoint. Build dial options: when c.cfg.TLS.Enabled, call
    connector.NewTLSConfig with the argusxdr TLS fields and wrap with
    credentials.NewTLS(tlsCfg) → grpc.WithTransportCredentials; otherwise (localhost test only)
    grpc.WithTransportCredentials(insecure.NewCredentials()). Add grpc.WithPerRPCCredentials of a
    small unexported type implementing credentials.PerRPCCredentials.GetRequestMetadata returning
    {"x-argus-api-key": c.cfg.Auth.Credential} and RequireTransportSecurity()==true when TLS enabled.
    Dial with grpc.NewClient(c.cfg.Endpoint, opts...); store conn and
    sdkv1.NewSDKIngestServiceClient(conn). Add a package-internal NewWithClient(cfg,
    client sdkv1.SDKIngestServiceClient) *Connector for tests. NO new dependency — all gRPC libs are
    already present. Write a bufconn-based test server (or loopback listener) registering an
    UnimplementedSDKIngestServiceServer-embedding fake; assert Connect/NewWithClient produce a usable
    client before implementing.
  </action>
  <verify>
    <automated>cd C:\Users\Drupad\argus-sdk && go test ./internal/connector/argusxdr/ -run "TestConnect|TestNewWithClient" -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Connect/NewWithClient tests pass.
    - `grep -v '^#' internal/connector/argusxdr/connector.go | grep -c "NewTLSConfig"` is non-zero (TLS via helper).
    - `grep -v '^#' internal/connector/argusxdr/connector.go | grep -c "WithPerRPCCredentials"` is non-zero (API-key creds).
    - go.mod gains no new module (grpc already required).
  </acceptance_criteria>
  <done>Connect dials gRPC with TLS 1.3 transport creds + per-RPC API key and yields a client stub.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Send — proto marshal + IngestBatch with delivery contract</name>
  <files>internal/connector/argusxdr/connector.go, internal/connector/argusxdr/connector_test.go</files>
  <read_first>internal/connector/argusxdr/connector.go, pkg/signal/signal.go, gen/go/sdk/v1/ingest.pb.go, gen/go/sdk/v1/ingest_grpc.pb.go</read_first>
  <behavior>
    - Send against an in-process server that returns a successful BatchAck returns
      DeliveryAck{Status:"delivered"} with BatchID echoed and RemoteID from BatchAck.BatchId.
    - Send against a server returning a gRPC error returns a non-nil error AND
      DeliveryAck{Status:"failed"} (delivery contract, locked decisions 5/9).
    - Send when the client stub is nil (Connect not called) returns a failed ack + error.
    - The proto SignalBatch sent contains all batch.Signals marshaled via signal.Batch.ToProto
      (verified by the test server inspecting the received SignalBatch.Signals length).
    - The proto SignalBatch.AppId/Env equal batch.AppID/batch.Env (the connector.SignalBatch fields
      populated by 04-05), NOT batch.Signals[0].AppID — verified by the test server reading AppId/Env
      while the test leaves Signals[0].AppID empty.
    - InstanceID and GroupID are sent as per-RPC metadata (x-argus-instance-id / x-argus-group-id),
      verified by the test server reading metadata.FromIncomingContext; they do NOT appear on the proto
      message (the proto has no such fields).
    - UseOCSF on the input batch is ignored — argusxdr always sends proto.
  </behavior>
  <action>
    Implement Send: guard nil client → failed ack + error. Reconstruct a signal.Batch from the
    connector.SignalBatch: AppID = batch.AppID, Env = batch.Env (the SignalBatch struct's own fields,
    populated by the 04-05 ingest loop — NEVER read Signals[0].AppID, which signal.FromProto leaves
    empty), Signals = batch.Signals; call (signal.Batch).ToProto(batch.BatchID, sdkVersion) where
    sdkVersion is a package constant (e.g. "argus-sdk/4"). ToProto sets proto BatchId/AppId/Env/Signals/
    SdkVersion — these are the ONLY five proto fields; there is no InstanceId/GroupId in the proto.
    Attach InstanceID and GroupID as per-RPC gRPC metadata: build an outgoing context with
    metadata.NewOutgoingContext(ctx, metadata.Pairs("x-argus-instance-id", batch.InstanceID,
    "x-argus-group-id", batch.GroupID)) before the call (these reuse the c.cfg.Auth.InstanceID/GroupID
    only as fallback if batch fields are empty). Optionally chunk by c.cfg.MaxBatchSize using sequential
    abort-on-first-failure to mirror the Phase 3 chunking contract. Call client.IngestBatch(mdCtx,
    protoBatch). On error return failed ack + wrapped error. On success return delivered ack with
    RemoteID = ack.BatchId. Ignore batch.UseOCSF entirely. Implement Health (call IngestBatch with an
    empty batch OR a lightweight check — document choice) and Close (conn.Close() if non-nil). Write
    Send happy-path and error-path tests using the in-process server from Task 1 before implementing;
    the happy-path test server must assert (a) received SignalBatch.AppId/Env match the batch fields,
    (b) x-argus-instance-id / x-argus-group-id are present in incoming metadata.
  </action>
  <verify>
    <automated>cd C:\Users\Drupad\argus-sdk && go test ./internal/connector/argusxdr/ -count=1 && go vet ./internal/connector/argusxdr/...</automated>
  </verify>
  <acceptance_criteria>
    - Full argusxdr package test suite passes Docker-free.
    - Happy-path test asserts the test server received len(batch.Signals) proto signals.
    - Happy-path test asserts proto AppId/Env equal batch.AppID/Env while Signals[0].AppID is empty.
    - Happy-path test asserts x-argus-instance-id and x-argus-group-id present in incoming metadata.
    - Error-path test asserts Send returns a non-nil error and ack.Status == "failed".
    - `grep -v '^#' internal/connector/argusxdr/connector.go | grep -c "ToProto"` is non-zero (locked marshal path).
    - `grep -v '^#' internal/connector/argusxdr/connector.go | grep -c "NewOutgoingContext"` is non-zero (metadata path).
  </acceptance_criteria>
  <done>argusxdr Send marshals via ToProto, sends InstanceID/GroupID as metadata, sources AppId/Env from the batch fields, calls IngestBatch, and obeys failed⇒error; no longer a scaffold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → ArgusXDR ingest | Signal proto + API-key credential cross the network over gRPC/TLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Spoofing | gRPC auth | mitigate | Per-RPC API-key creds; RequireTransportSecurity true when TLS enabled so key never sent in clear |
| T-04-08 | Information disclosure | TLS transport | mitigate | TLS 1.3 via connector.NewTLSConfig → credentials.NewTLS; no insecure creds outside loopback tests |
| T-04-09 | Repudiation | silent drop | mitigate | gRPC error ⇒ non-nil error + failed ack; no fake delivered |
| T-04-10 | Tampering | OCSF bypass | mitigate | UseOCSF ignored; argusxdr always uses the locked ToProto wire format (no alternate path) |
| T-04-11 | Spoofing | instance/group identity | mitigate | InstanceID/GroupID sent as per-RPC metadata over the TLS-secured channel; not forgeable in the proto body |
</threat_model>

<verification>
cd C:\Users\Drupad\argus-sdk && go build ./... && go vet ./... && go test ./internal/connector/argusxdr/ -count=1 -timeout 120s
</verification>

<success_criteria>
- SC-8: argusxdr Send() marshals signals to sdkv1.SignalBatch (five proto fields only) and calls
  SDKIngestService.IngestBatch over gRPC with TLS + API-key creds; InstanceID/GroupID are sent as
  per-RPC metadata; AppId/Env come from the connector.SignalBatch fields; unit-tested against an
  in-process gRPC server; failed delivery returns a non-nil error; UseOCSF=false always; no new dependency.
</success_criteria>

<output>
Create .planning/workstreams/ws-b-connector-layer/phases/04-agent-wiring/04-03-SUMMARY.md when done
</output>
</content>
</invoke>
