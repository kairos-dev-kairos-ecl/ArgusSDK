---
phase: "01-proto-codegen-signal-alignment"
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - proto/sdk/v1/ingest.proto
autonomous: true
requirements:
  - SPEC-3
must_haves:
  truths:
    - "proto/sdk/v1/ingest.proto exists with package sdk.v1 and option go_package set to github.com/kairos-dev-kairos-ecl/ArgusSDK/gen/go/sdk/v1;sdkv1"
    - "SDKIngestService declares exactly one RPC: rpc IngestBatch(SignalBatch) returns (BatchAck)"
    - "SDKSignal has exactly 14 fields with the correct field numbers (signal_id=1 through app_version=14)"
    - "timestamp field in SDKSignal is google.protobuf.Timestamp (not int64)"
    - "proto file imports google/protobuf/timestamp.proto and no other external proto"
  artifacts:
    - path: "proto/sdk/v1/ingest.proto"
      provides: "Wire contract for SDK→agent ingest"
      contains: "SDKIngestService"
  key_links:
    - from: "proto/sdk/v1/ingest.proto"
      to: "gen/go/sdk/v1/"
      via: "buf generate (Plan 01-02)"
      pattern: "SDKIngestService"
---

<objective>
Write the locked SPEC-3 proto definition to proto/sdk/v1/ingest.proto. This is the wire contract between instrumentation libraries (Python/TypeScript/Go) and the argus-agent. All field numbers are frozen — changing them after WS-B, WS-E, or WS-G work begins would require coordinated migration across every consumer.

Purpose: Establish the stable wire contract that unblocks WS-B, WS-C, WS-D, WS-E, and WS-G.
Output: proto/sdk/v1/ingest.proto — a compilable proto3 file with SDKIngestService, SignalBatch, SDKSignal, BatchAck, RejectionDetail messages exactly as specified in SPEC-3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/ws-a-proto-foundation/ROADMAP.md
@.planning/workstreams/ws-a-proto-foundation/phases/01-proto-codegen-signal-alignment/01-CONTEXT.md
@.planning/WORKSTREAMS.md

<interfaces>
<!-- Locked proto definition from WORKSTREAMS.md SPEC-3 — implement this EXACTLY -->
<!-- All field numbers are frozen. Do not renumber, reorder, or rename. -->

Service:
  SDKIngestService
    rpc IngestBatch(SignalBatch) returns (BatchAck)  // UNARY — not streaming

Message SignalBatch:
  string batch_id    = 1;   // ULID assigned by SDK lib
  string app_id      = 2;   // validated against agent app registry
  string env         = 3;   // "dev" | "staging" | "prod"
  repeated SDKSignal signals = 4;   // recommended max 500
  string sdk_version = 5;

Message SDKSignal (14 fields total):
  string signal_id      = 1;   // ULID
  string trace_id       = 2;
  string span_id        = 3;
  string parent_span_id = 4;
  int32  layer          = 5;   // 1=L1_HARDWARE ... 10=L10_APPLICATION, 11=L_DECISION
  string category       = 6;
  int32  severity       = 7;   // 1=INFO ... 5=CRITICAL
  google.protobuf.Timestamp timestamp = 8;
  float  duration_ms    = 9;
  bytes  context_json   = 10;  // JSON-encoded layer context
  string session_id     = 11;
  string conversation_id = 12;
  string user_id        = 13;  // must be hashed by SDK lib before sending
  string app_version    = 14;

Message BatchAck:
  string batch_id       = 1;
  int32  accepted_count = 2;
  int32  rejected_count = 3;
  repeated RejectionDetail rejections = 4;

Message RejectionDetail:
  string signal_id = 1;
  string reason    = 2;   // e.g. "invalid layer: 0 (unspecified)"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create proto/sdk/v1/ingest.proto with locked SPEC-3 definition</name>
  <files>proto/sdk/v1/ingest.proto</files>
  <read_first>
    - .planning/workstreams/ws-a-proto-foundation/phases/01-proto-codegen-signal-alignment/01-CONTEXT.md (full decisions section — all field numbers)
    - .planning/WORKSTREAMS.md (section "A1 SDK Ingest Proto" — locked proto definition block)
  </read_first>
  <action>
    Create directory proto/sdk/v1/ and write ingest.proto with the following exact structure (do NOT use a fenced code block in this action — implement the file directly):

    Header:
    - syntax = "proto3"
    - package sdk.v1
    - import "google/protobuf/timestamp.proto"
    - option go_package = "github.com/kairos-dev-kairos-ecl/ArgusSDK/gen/go/sdk/v1;sdkv1"

    File-level doc comment block (above the service declaration) must describe:
    - Transport options: Unix socket path (Linux/macOS) and TCP loopback 127.0.0.1:5002 (all platforms including Windows where Unix sockets are unsupported)
    - Auth model: OS file permissions (0660) on Unix socket; app_id validation against agent registry on TCP path; no cryptographic auth on local SDK→agent hop
    - Windows note: agent Start() skips net.Listen("unix", ...) when runtime.GOOS == "windows"

    Service SDKIngestService with one RPC:
    - rpc IngestBatch(SignalBatch) returns (BatchAck)
    - RPC comment: "Unary — blocks until agent accepts batch; context deadline enforces the <5ms budget."

    Message SignalBatch (5 fields, field numbers 1–5 exactly as specified in the interfaces block above). Comment on each field per the interfaces block.

    Message SDKSignal (14 fields, field numbers 1–14 exactly as specified). Group fields into inline comment sections: "Identity", "Classification — numeric values match argus.v1 enums (zero-cost int32 cast)", "Temporal", "Layer-specific payload — JSON bytes; agent validates structure per declared layer", "Relationship", "Source (app_id + env come from SignalBatch)". Per the locked decision, layer and severity are int32 NOT imported enums.

    Message BatchAck (4 fields, numbers 1–4 exactly as specified). Comment: "Present only when rejected_count > 0" on the rejections field.

    Message RejectionDetail (2 fields, numbers 1–2 exactly as specified). Comment on reason field: e.g. "invalid layer: 0 (unspecified)", "context_json: invalid JSON".

    All comments must be sentence-case, period-terminated per the discretion note in CONTEXT.md.
  </action>
  <verify>
    <automated>grep -c "SDKIngestService\|SignalBatch\|SDKSignal\|BatchAck\|RejectionDetail" proto/sdk/v1/ingest.proto</automated>
    Expected: output is 5 (each message/service name appears at least once)

    <automated>grep "option go_package" proto/sdk/v1/ingest.proto</automated>
    Expected output contains: github.com/kairos-dev-kairos-ecl/ArgusSDK/gen/go/sdk/v1;sdkv1

    <automated>grep "app_version = 14" proto/sdk/v1/ingest.proto</automated>
    Expected: line found (confirms all 14 SDKSignal fields present)

    <automated>grep "google.protobuf.Timestamp timestamp = 8" proto/sdk/v1/ingest.proto</automated>
    Expected: line found (confirms Timestamp type, not int64)

    <automated>grep "import" proto/sdk/v1/ingest.proto</automated>
    Expected: exactly one import line — google/protobuf/timestamp.proto — no others
  </verify>
  <done>
    - proto/sdk/v1/ingest.proto exists
    - File opens with syntax = "proto3" and package sdk.v1
    - go_package option is exactly "github.com/kairos-dev-kairos-ecl/ArgusSDK/gen/go/sdk/v1;sdkv1"
    - All 5 message types present: SignalBatch, SDKSignal, BatchAck, RejectionDetail, plus service SDKIngestService
    - SDKSignal has fields signal_id=1 through app_version=14 (14 fields total, none skipped)
    - timestamp field type is google.protobuf.Timestamp (not int64 or uint64)
    - layer (field 5) and severity (field 7) are int32, NOT enum types
    - Only one proto import: google/protobuf/timestamp.proto
    - Transport and auth doc comment present above service declaration
    - RPC IngestBatch is unary (no stream keyword on request or response)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proto file → buf codegen | buf reads proto source and generates Go; malformed proto causes codegen failure, not a security boundary |
| proto field comments → SDK consumers | Comments document auth model; incorrect comments could mislead implementors but have no runtime effect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | proto/sdk/v1/ingest.proto field numbers | mitigate | Field numbers are LOCKED in SPEC-3; any renumbering is a breaking change caught by buf breaking-change detection in Plan 01-02 CI setup |
| T-01-02 | Information Disclosure | user_id field (field 13) | accept | Field comment documents that SDK lib must hash before sending; enforcement is a WS-E runtime concern, not proto schema |
| T-01-SC | Tampering | No npm/pip/cargo installs in this plan | accept | No package manager installs; no slopcheck required |
</threat_model>

<verification>
Run after task completes:

1. Check file exists: ls proto/sdk/v1/ingest.proto
2. Verify proto syntax (if protoc available): protoc --proto_path=proto proto/sdk/v1/ingest.proto --descriptor_set_out=/dev/null
3. Grep-verify all message names present: grep -E "^(service|message)" proto/sdk/v1/ingest.proto
   Expected lines: service SDKIngestService, message SignalBatch, message SDKSignal, message BatchAck, message RejectionDetail
4. Confirm no streaming RPCs: grep "stream" proto/sdk/v1/ingest.proto should return empty
5. Confirm 14 SDKSignal fields by counting lines matching "= [0-9]" inside the SDKSignal message block
</verification>

<success_criteria>
- proto/sdk/v1/ingest.proto exists and is non-empty
- All SPEC-3 message types present with exact field numbers (verified by grep checks above)
- No streaming keyword on IngestBatch RPC
- google.protobuf.Timestamp used for timestamp field (not int64)
- layer and severity are int32 (not imported enum types)
- go_package option matches module path github.com/kairos-dev-kairos-ecl/ArgusSDK/gen/go/sdk/v1;sdkv1
- File is ready for buf generate to consume (Plan 01-02)
</success_criteria>

<output>
Create .planning/workstreams/ws-a-proto-foundation/phases/01-proto-codegen-signal-alignment/01-01-SUMMARY.md when done.
</output>
