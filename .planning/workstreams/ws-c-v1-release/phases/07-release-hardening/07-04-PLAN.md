---
phase: 07-release-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - R-76   # CI completeness: build/vet/CGO -race/integration + Phase-6 cross-compile/isolation matrix; local verification documented
must_haves:
  truths:
    - "CI defines build+vet, a CGO-enabled -race job, and an integration job on a real ubuntu runner, triggered on push and pull_request"
    - "CI includes a Phase-6 cross-compile/isolation matrix over GOOS linux/windows/darwin that asserts eBPF/ETW-specific code is absent from non-target builds (a non-target build does not pull in another platform's native observer)"
    - "The race job sets CGO_ENABLED=1 and runs go test -race ./... — it is the authoritative race gate (the dev box has no C compiler)"
    - "setup-go is pinned to the go.mod toolchain (go 1.26.1) across all jobs"
    - "The workflow YAML is syntactically valid and every job is reachable"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Complete CI: build+vet, CGO -race, integration, cross-compile/isolation matrix"
      contains: "CGO_ENABLED"
  key_links:
    - from: ".github/workflows/ci.yml race job"
      to: "go test -race ./..."
      via: "CGO_ENABLED=1 on ubuntu-latest"
      pattern: "go test -race"
    - from: ".github/workflows/ci.yml cross-compile matrix"
      to: "GOOS linux/windows/darwin builds"
      via: "matrix.goos build + isolation assertion"
      pattern: "GOOS"
---

<objective>
Complete .github/workflows/ci.yml so CI proves the v1.0 release on a real runner, satisfying ROADMAP Phase 7 Success Criterion 6 / R-76 and the 07-CONTEXT LOCKED "CI" decision.

The existing ci.yml already has build-vet, race (CGO_ENABLED=1), and integration jobs. This plan completes it by adding the Phase-6 cross-compile / isolation matrix and verifying the whole workflow is correct and locally-verifiable where possible:

LOCKED requirements:
- Keep build+vet, the CGO -race gate, and the integration job; ensure all are correct and triggered on push + pull_request.
- ADD the Phase-6 cross-compile/isolation matrix: build for GOOS linux, windows, darwin and ASSERT that platform-native observers (eBPF on linux, ETW/WFP on windows, Network Extension on darwin) are absent from NON-target builds — i.e. a windows build must not link the linux eBPF observer, etc. The EUC OS impls are behind build tags (linux.go //go:build linux, windows.go, darwin.go), so cross-builds already exclude off-target code; the matrix proves it stays that way and that each GOOS builds cleanly.
- The -race and integration jobs run on the runner (push); LOCALLY verify everything verifiable here (build, vet, non-race test, cross-compile builds) and document that -race + integration execute on CI (the dev box is Windows, no C compiler, possibly no Docker).

Constraints (binding):
- This plan touches ONLY .github/workflows/ci.yml. File-disjoint from 07-01/02/03 → Wave 1.
- Do NOT run -race locally (no C compiler) — assert it via the workflow file + run the non-race local checks.
- The EUC seam from 07-02 must not have broken cross-compilation; the matrix's per-GOOS build is the guard. (07-02 is Wave 1 and file-disjoint; if it is not yet merged when this runs, the matrix is still correct — it builds whatever is on the tree.)
- Keep setup-go pinned to 1.26.1 (go.mod toolchain). Trigger on push + pull_request.

Purpose: the dev machine cannot run the race detector or guarantee Docker; CI is the authoritative gate. The cross-compile/isolation matrix is what keeps the platform-tagged EUC observers from leaking into the wrong OS build — a real correctness property of the Phase-6 design.
Output: a complete, valid CI workflow with the cross-compile/isolation matrix, plus documented local verification evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/ws-c-v1-release/ROADMAP.md
@.planning/workstreams/ws-c-v1-release/phases/07-release-hardening/07-CONTEXT.md
@.github/workflows/ci.yml
@Makefile
@internal/collector/euc/euc.go
@internal/collector/euc/linux.go
@internal/collector/euc/euc_noop.go

<interfaces>
Current ci.yml (verified 2026-06-12) already defines:
  - build-vet (ubuntu-latest): go build ./...; go vet ./...; go vet -tags=integration for kafka/elastic/splunk.
  - race (ubuntu-latest, env CGO_ENABLED: "1"): go test -race ./... -count=1 -timeout 300s.
  - integration (ubuntu-latest): make test-int (timeout 15m).
  All use actions/checkout@v4 + actions/setup-go@v5 go-version "1.26.1", triggered on push/pull_request "**".

What is MISSING and this plan adds:
  - A cross-compile / isolation matrix job.

EUC build tags (the isolation property to assert):
  - internal/collector/euc/linux.go:  //go:build linux  → linuxCollector (eBPF observer, newOSCollector).
  - internal/collector/euc/windows.go: //go:build windows → windowsCollector (ETW/WFP).
  - internal/collector/euc/darwin.go:  //go:build darwin → darwinCollector (Network Extension).
  - internal/collector/euc/euc_noop.go: no build tag → noop fallback compiled everywhere.
  So `GOOS=windows go build ./...` MUST compile windows.go and exclude linux.go/darwin.go automatically; the matrix verifies each GOOS builds and (optional, stronger) that the per-OS file set is what compiles.

Cross-compile isolation assertion approaches (choose one, document it):
  (a) Simplest/robust: matrix over goos=[linux,windows,darwin], each runs `GOOS=${{matrix.goos}} GOARCH=amd64 go build ./...` (darwin: arm64 also acceptable). A clean build per GOOS proves the tagged files compile in isolation and off-target files are excluded by the build constraints. Add a `go vet` per GOOS where feasible.
  (b) Stronger isolation evidence: additionally run `go list -tags '' -f '{{.GoFiles}}' ./internal/collector/euc/` under each GOOS and assert the output contains only that platform's file + euc.go/euc_noop.go and NOT the other platforms' files (e.g. a windows GOOS listing must not contain linux.go). Implement as a shell step that greps the go list output and `exit 1` on a leaked file.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase-6 cross-compile / isolation matrix to ci.yml and validate the full workflow</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (existing build-vet/race/integration jobs)
    - internal/collector/euc/linux.go (//go:build linux), and confirm windows.go/darwin.go/euc_noop.go tags
    - Makefile (build-all target :22-26 — the existing cross-compile targets for reference)
  </read_first>
  <action>
    Add a new job `cross-compile` (ubuntu-latest) to .github/workflows/ci.yml using a matrix over goos: [linux, windows, darwin]. For each goos:
    - actions/checkout@v4 + actions/setup-go@v5 with go-version "1.26.1" (match the other jobs).
    - Build step: `GOOS=${{ matrix.goos }} GOARCH=amd64 go build ./...` (for darwin you may use GOARCH=arm64 to match build-all; pick one and keep it consistent). This proves each platform's tagged EUC observer compiles in isolation and off-target files are excluded by build constraints.
    - Isolation assertion step (approach b, stronger): run `GOOS=${{ matrix.goos }} go list -f '{{ join .GoFiles "\n" }}' ./internal/collector/euc/` and assert the listing INCLUDES euc.go + euc_noop.go + the matching platform file, and EXCLUDES the other two platform files. Implement as a shell step that captures the output and `exit 1` if a non-matching platform file (e.g. linux.go in a windows listing) appears. Document the exact grep logic inline in the YAML comment.
    Then validate the whole workflow: confirm build-vet, race (CGO_ENABLED=1), integration, and the new cross-compile job are all present, all on ubuntu-latest, all triggered on push + pull_request, all pinned to go 1.26.1. Ensure the YAML is syntactically valid.
    Document in the SUMMARY: the local verification performed (go build ./..., go vet ./..., non-race go test ./..., and the three GOOS cross-builds run locally on Windows via env GOOS=...), and the explicit note that the CGO -race job and the Docker integration job execute on the CI runner (not locally) — known-good evidence.
  </action>
  <acceptance_criteria>
    - ci.yml contains a cross-compile job with a matrix over goos linux/windows/darwin, each doing a GOOS-scoped `go build ./...`.
    - ci.yml contains an isolation assertion step that fails (exit 1) if an off-target EUC platform file appears in the per-GOOS `go list` of internal/collector/euc.
    - build-vet, race (CGO_ENABLED=1, go test -race ./...), and integration (make test-int) jobs are all present and correct.
    - All jobs use actions/setup-go@v5 with go-version "1.26.1" and the workflow triggers on push + pull_request.
    - The workflow YAML is valid (parses) and every job is reachable.
    - Local verification passes: `go build ./...`, `go vet ./...`, non-race `go test ./...`, and the three `GOOS=... go build ./...` cross-builds all succeed locally.
    - `cd C:\Users\Drupad\argus-sdk && grep -c "CGO_ENABLED" .github/workflows/ci.yml` >= 1; `grep -c "matrix" .github/workflows/ci.yml` >= 1; `grep -c "GOOS" .github/workflows/ci.yml` >= 1.
  </acceptance_criteria>
  <verify>
    <automated>cd C:\Users\Drupad\argus-sdk && go build ./... && go vet ./... && go test ./... -count=1 -timeout 300s && env GOOS=linux GOARCH=amd64 go build ./... && env GOOS=windows GOARCH=amd64 go build ./... && env GOOS=darwin GOARCH=arm64 go build ./... && grep -c "CGO_ENABLED" .github/workflows/ci.yml && grep -c "matrix" .github/workflows/ci.yml</automated>
  </verify>
  <done>ci.yml has build-vet + CGO -race + integration + a cross-compile/isolation matrix asserting EUC platform-file isolation; all three GOOS cross-builds and non-race local checks pass; -race/integration documented as CI-run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → container registry / module proxy | CI pulls Docker images (integration) and Go modules (build) |
| Build constraints (//go:build tags) → per-OS binary contents | Mis-tagged code could leak a platform-native observer into the wrong OS binary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11 | Tampering | EUC platform observer leaks into wrong-OS build | mitigate | Cross-compile matrix + go list isolation assertion fails CI if an off-target platform file compiles into a build |
| T-07-12 | Denial of Service | undetected data race ships in v1.0 | mitigate | CGO-enabled -race job on ubuntu is the authoritative gate on every push/PR |
| T-07-13 | Tampering (supply chain) | unpinned toolchain/actions | mitigate | actions pinned to @v4/@v5 majors; go toolchain pinned to 1.26.1 matching go.mod |
| T-07-SC | Tampering | package installs | accept | CI-config only — no application dependency added; integration job uses the already-vetted testcontainers-go (test-only) |
</threat_model>

<verification>
cd C:\Users\Drupad\argus-sdk && go build ./... && go vet ./...
cd C:\Users\Drupad\argus-sdk && go test ./... -count=1 -timeout 300s
cd C:\Users\Drupad\argus-sdk && env GOOS=linux GOARCH=amd64 go build ./... && env GOOS=windows GOARCH=amd64 go build ./... && env GOOS=darwin GOARCH=arm64 go build ./...
# -race + integration are NOT run locally (no C compiler / Docker on the dev box); they are asserted by ci.yml and run on the CI runner.
</verification>

<success_criteria>
- ci.yml runs build, vet, CGO -race, and integration on a real ubuntu runner, plus a Phase-6 cross-compile/isolation matrix over linux/windows/darwin (R-76, SC-6).
- The isolation step fails CI if an off-target EUC platform file leaks into a build.
- setup-go pinned to 1.26.1; triggers on push + pull_request; YAML valid.
- Local verification (build/vet/non-race test/3x cross-build) passes; -race + integration documented as CI-run.
- No application code or dependency changed.
</success_criteria>

<output>
Create .planning/workstreams/ws-c-v1-release/phases/07-release-hardening/07-04-SUMMARY.md when done
</output>
</content>
