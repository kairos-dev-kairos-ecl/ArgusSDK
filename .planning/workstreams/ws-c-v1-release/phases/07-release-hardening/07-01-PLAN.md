---
phase: 07-release-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/ocsf/mapper.go
  - internal/ocsf/mapper_test.go
autonomous: true
requirements:
  - R-71   # OCSF fidelity: web_resources + databucket first-class; real URL or omit; injectable clock; round-trip tests
must_haves:
  truths:
    - "The Mapper has an injectable clock — tests construct a Mapper with a fixed clock and assert metadata.logged_time is exactly the injected instant; default construction still uses real wall-clock time"
    - "Web Resources Activity (6001) events carry a first-class web_resources object (not a stringy Unmapped[\"web_resource_url\"]) when context.resource_url is present; the object is omitted when no resource is known"
    - "Datastore Activity (6005) events carry a first-class databucket object (not Unmapped[\"databucket_name\"]) when context.vector_index is present; omitted otherwise"
    - "HTTP Activity (4002) http_request.url is a real URL derived from context (url / target / path) — never s.Category; when no URL is derivable the field is the empty string / omitted rather than mis-populated with the category"
    - "All affected OCSF classes (6001, 6005, 4002) round-trip through Map in unit tests; no pre-existing mapper test regresses — including TestMap_HTTPActivity, whose no-context case is updated to assert http_request.url == \"\" (NOT s.Category)"
  artifacts:
    - path: "internal/ocsf/mapper.go"
      provides: "Injectable clock on Mapper; first-class WebResources + Databucket objects on Event; URL derived from context, no s.Category fallback"
      contains: "WebResources"
    - path: "internal/ocsf/mapper_test.go"
      provides: "Fixed-clock determinism test; first-class web_resources/databucket round-trip tests; URL-derivation + URL-absent tests; TestMap_HTTPActivity updated so the no-context case asserts URL == \"\""
      contains: "logged_time"
  key_links:
    - from: "internal/ocsf/mapper.go Map()"
      to: "Mapper.clock"
      via: "metadata.LoggedTime = m.clock()"
      pattern: "m\\.clock\\(\\)"
    - from: "internal/ocsf/mapper.go populateClassFields()"
      to: "Event.WebResources / Event.Databucket"
      via: "first-class object set from extractCtx, replacing Unmapped string keys"
      pattern: "ev\\.(WebResources|Databucket)"
---

<objective>
Close the OCSF fidelity gaps in internal/ocsf/mapper.go so v1.0 OCSF output is complete and deterministic for the in-scope classes (per 07-CONTEXT LOCKED "OCSF fidelity" decisions and ROADMAP Phase 7 Success Criterion 1 / R-71):

- mapper.go:312-315 — the F17 deferral. The Mapper's LoggedTime uses time.Now().UTC(), making Map non-deterministic and not golden-testable. RESOLVE it here (the deferral expires in Phase 7): make the clock injectable (default real time; tests inject a fixed clock).
- mapper.go:396 — the HTTP url placeholder. `url := s.Category` mis-populates http_request.url with the category whenever context has no url. Derive a real URL from context where available, else leave the field empty rather than mislabel it.
- mapper.go:435-447 (Datastore 6005) — the databucket TODO. context.vector_index is stuffed into Unmapped["databucket_name"]. Promote it to a first-class databucket object on the Event.
- mapper.go:449-460 (Web Resources 6001) — the web_resources deferral. context.resource_url is stuffed into Unmapped["web_resource_url"]. Promote it to a first-class web_resources object on the Event.

Constraints (from 07-CONTEXT, binding):
- Follow OCSF v1.3 + the existing mapper conventions for object shapes (Claude's discretion on exact shape, within OCSF v1.3).
- Do NOT regress existing mapper tests (TestMap_AllLayers, TestMap_DatastoreActivity_ContextJSON_VectorIndex, TestMap_WebResources_ContextJSON_ResourceURL, TestMap_Activity99SetsActivityName, etc.). The two ContextJSON tests at mapper_test.go:433 and :450 assert the OLD Unmapped behaviour — update them to assert the new first-class objects (this is the intended fidelity change, not a regression). ADDITIONALLY: TestMap_HTTPActivity (mapper_test.go:264) maps a no-context signal and currently asserts http_request.url is NON-empty (:274-276) — this assertion relied on the s.Category fallback and WILL FAIL once the fallback is removed. Update that test's no-context case to assert http_request.url == "" (and explicitly that it is NOT s.Category). This is part of the honesty fix, not a regression.
- This plan touches ONLY internal/ocsf/* — it is file-disjoint from 07-02/03/04 and runs in Wave 1.

Purpose: OCSF output is the wire contract for every Mode-2 (SIEM) destination; dropping web_resources/databucket and mislabeling URLs makes Argus events fail OCSF validators and lose analyst fidelity. The injectable clock unblocks golden testing for the whole package.
Output: a deterministic, injectable-clock Mapper; first-class WebResources + Databucket objects; honest URLs; extended round-trip tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/ws-c-v1-release/ROADMAP.md
@.planning/workstreams/ws-c-v1-release/phases/07-release-hardening/07-CONTEXT.md
@internal/ocsf/mapper.go
@internal/ocsf/mapper_test.go

<interfaces>
Current code the executor must know (verified against source 2026-06-12):

Mapper (mapper.go:252-266):
  type Mapper struct { productVersion string; agentHostname string }
  func NewMapper(productVersion, agentHostname string) *Mapper  // public API — keep this signature working

Map() (mapper.go:270-342):
  - mapper.go:315: LoggedTime: time.Now().UTC()  // the clock to make injectable (F17 deferral, now resolved)
  - activityName/ActivityName for activity_id==99 is ALREADY done (mapper.go:287-292, 300) — do NOT touch.

Event (mapper.go:80-136): struct of OCSF fields with json tags. New first-class objects are added here:
  - currently NO web_resources or databucket field exists.
  - Unmapped map[string]interface{} `json:"unmapped,omitempty"` — the current dumping ground for databucket_name / web_resource_url.

populateClassFields() (mapper.go:361-462):
  - ClassHTTPActivity (4002, mapper.go:391-424): url := s.Category fallback at :396; overridden by ctx["url"] when present.
  - ClassDatastoreActivity (6005, mapper.go:435-447): ev.Unmapped["databucket_name"] = ctx["vector_index"].
  - ClassWebResourcesActivity (6001, mapper.go:449-460): ev.Unmapped["web_resource_url"] = ctx["resource_url"].
  - extractCtx(s.ContextJSON) map[string]interface{} helper already exists (mapper.go:347-356).

mapper_test.go relevant tests:
  - :13 newTestMapper() returns NewMapper("1.0.0","test-host") — extend or add a fixed-clock variant.
  - :264 TestMap_HTTPActivity — maps a no-context L9APIGateway signal; asserts http_request fields. At :274-276 it asserts URL is NON-empty (depends on the s.Category fallback). UPDATE the no-context case to assert ev.HttpRequest.URL == "" (and NOT s.Category) once the fallback is removed.
  - :433 TestMap_DatastoreActivity_ContextJSON_VectorIndex — currently asserts Unmapped["databucket_name"]. UPDATE to assert the first-class object.
  - :450 TestMap_WebResources_ContextJSON_ResourceURL — currently asserts Unmapped["web_resource_url"]. UPDATE to assert the first-class object.

OCSF v1.3 object shape guidance (Claude's discretion, follow these conventions):
  - web_resources is an ARRAY of web_resource objects on the event (json key "web_resources"); each has at least url_string (json "url_string") and optionally name. Use a []WebResource with one element.
  - databucket is a single object (json key "databucket") with at least name (json "name") and optionally type/uid. Use a *Databucket.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Injectable clock on Mapper (resolve F17)</name>
  <files>internal/ocsf/mapper.go, internal/ocsf/mapper_test.go</files>
  <read_first>
    - internal/ocsf/mapper.go (Mapper struct :252-266, NewMapper :261-266, Map LoggedTime :315)
    - internal/ocsf/mapper_test.go (:13 newTestMapper, :101 TestMap_AllSignalFields, :350 TestMap_ContextJSON_RoundTrip)
  </read_first>
  <behavior>
    - TestMap_InjectedClockIsDeterministic: construct a Mapper whose clock returns a FIXED time (e.g. 2024-01-02T03:04:05Z). Map any valid signal twice; assert ev.Metadata.LoggedTime equals the fixed time exactly on both calls (no drift). This proves Map is now golden-testable.
    - TestNewMapper_DefaultClockIsRealTime (regression): NewMapper(...) (the existing public constructor) still produces a LoggedTime within a few seconds of time.Now().UTC() — the default path is unchanged for existing callers.
  </behavior>
  <action>
    Add an injectable clock to Mapper. Add an unexported field `clock func() time.Time` to the Mapper struct. In NewMapper, default it to `func() time.Time { return time.Now().UTC() }` so the existing public signature and behaviour are unchanged. Add a test-facing constructor `NewMapperWithClock(productVersion, agentHostname string, clock func() time.Time) *Mapper` (or an unexported `newMapperWithClock` used from the _test.go in package ocsf) that injects the clock; if clock is nil fall back to the real-time default. In Map(), replace `LoggedTime: time.Now().UTC()` (mapper.go:315) with `LoggedTime: m.clock()`. DELETE the F17 NOTE comment block (mapper.go:312-314) since the deferral is now resolved; the SUMMARY must record that F17 is closed (no longer deferred). Confirm no other time.Now() call remains in Map (grep `time.Now` in mapper.go — only the default-clock closure should reference it).
  </action>
  <acceptance_criteria>
    - Mapper has a `clock func() time.Time` field; NewMapper defaults it to real UTC time and its signature is unchanged.
    - A clock-injecting constructor exists and is used by the determinism test.
    - mapper.go:315 reads `LoggedTime: m.clock()` (or equivalent) — no direct time.Now() in the Event literal.
    - The F17 deferral comment is removed.
    - `cd C:\Users\Drupad\argus-sdk && go test ./internal/ocsf/... -count=1` passes including the two new tests.
  </acceptance_criteria>
  <verify>
    <automated>cd C:\Users\Drupad\argus-sdk && go build ./internal/ocsf/... && go test ./internal/ocsf/... -count=1 -run "Clock|Mapper|AllLayers" 2>&1</automated>
  </verify>
  <done>Mapper clock is injectable; fixed-clock test is deterministic; default constructor unchanged; F17 comment gone; ocsf tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: First-class web_resources + databucket; honest HTTP URL</name>
  <files>internal/ocsf/mapper.go, internal/ocsf/mapper_test.go</files>
  <read_first>
    - internal/ocsf/mapper.go (Event struct :80-136, populateClassFields HTTP :391-424, Datastore :435-447, WebResources :449-460, extractCtx :347-356)
    - internal/ocsf/mapper_test.go (:264 TestMap_HTTPActivity — asserts URL NON-empty at :274-276 on a no-context signal, WILL regress when the s.Category fallback is removed; :315 TestMap_DatastoreActivity, :433 VectorIndex, :450 ResourceURL)
  </read_first>
  <behavior>
    web_resources (6001):
    - TestMap_WebResources_FirstClassObject: Map an L10Application signal with ContextJSON {"resource_url":"https://app.example.com/dashboard"}; assert ev.WebResources is a non-empty slice whose [0].URLString == the resource_url; assert Unmapped does NOT contain "web_resource_url".
    - TestMap_WebResources_OmittedWhenNoURL: Map an L10Application signal with no resource_url in context; assert ev.WebResources is nil/empty (omitted) — no placeholder.
    databucket (6005):
    - TestMap_Datastore_FirstClassDatabucket: Map an L7RAGRetrieval signal with ContextJSON {"vector_index":"prod-embeddings"}; assert ev.Databucket != nil && ev.Databucket.Name == "prod-embeddings"; assert Unmapped does NOT contain "databucket_name".
    - TestMap_Datastore_DatabucketOmittedWhenAbsent: no vector_index → ev.Databucket == nil.
    HTTP url (4002):
    - TestMap_HTTP_URLFromContext: ContextJSON {"url":"https://api.example.com/v1/chat"} → ev.HttpRequest.URL == that url.
    - TestMap_HTTP_URLNotCategoryWhenAbsent: L9APIGateway signal with Category "api.request" and NO url in context → ev.HttpRequest.URL != s.Category (must be "" or a value derived from context.target/path, never the category). HttpRequest is still constructed (method/code still required by 4002), only URL is honest.
    UPDATE the existing test TestMap_HTTPActivity (mapper_test.go:264): it maps a no-context signal and at :274-276 asserts URL is non-empty. Change that no-context assertion to assert ev.HttpRequest.URL == "" (and explicitly that it is NOT equal to s.Category) — the no-context case now yields an empty, honest URL. Keep the method/response/dst_endpoint assertions intact.
    Update the two existing tests at mapper_test.go:433 and :450 to assert the first-class objects instead of the old Unmapped keys.
  </behavior>
  <action>
    Add OCSF v1.3 object types and fields (Claude's discretion on exact shape, follow OCSF v1.3 + interfaces note):
    - Add `type WebResource struct { URLString string `json:"url_string,omitempty"`; Name string `json:"name,omitempty"` }` and on Event add `WebResources []WebResource `json:"web_resources,omitempty"``.
    - Add `type Databucket struct { Name string `json:"name,omitempty"`; Type string `json:"type,omitempty"`; UID string `json:"uid,omitempty"` }` and on Event add `Databucket *Databucket `json:"databucket,omitempty"``.
    web_resources (populateClassFields ClassWebResourcesActivity): replace the Unmapped["web_resource_url"] assignment with: parse ctx["resource_url"] (string, non-empty) → ev.WebResources = []WebResource{{URLString: url}}. If absent, leave ev.WebResources nil (omitted). Do NOT write to Unmapped for this key anymore.
    databucket (ClassDatastoreActivity): replace Unmapped["databucket_name"] with: parse ctx["vector_index"] (string, non-empty) → ev.Databucket = &Databucket{Name: name}. If absent, ev.Databucket stays nil. (Datastore 6005 at_least_one constraint is satisfied by the databucket when present; src_endpoint remains as today.)
    HTTP url (ClassHTTPActivity): remove `url := s.Category` (mapper.go:396). Initialise `url := ""`; derive from context in priority order: ctx["url"], else ctx["target"], else ctx["path"] (each: string, non-empty). Leave url == "" when none present. Keep method and statusCode fallbacks as today (4002 requires method + response.code; URL honesty is the only change). HttpRequest.URL = url. Grep mapper.go to confirm no remaining `s.Category` assignment to a URL field.
    Update mapper_test.go: (1) TestMap_HTTPActivity (:264) no-context case — change the URL assertion from "non-empty" to assert ev.HttpRequest.URL == "" and != s.Category; (2) :433 (VectorIndex) → assert ev.Databucket and that Unmapped["databucket_name"] is gone; (3) :450 (ResourceURL) → assert ev.WebResources and that Unmapped["web_resource_url"] is gone.
  </action>
  <acceptance_criteria>
    - Event has WebResources []WebResource and Databucket *Databucket fields with OCSF v1.3 json tags (web_resources, databucket).
    - 6001 events set WebResources from context.resource_url; omitted when absent; Unmapped no longer carries web_resource_url.
    - 6005 events set Databucket from context.vector_index; omitted when absent; Unmapped no longer carries databucket_name.
    - 4002 http_request.url is derived from context (url/target/path) or empty — never s.Category.
    - TestMap_HTTPActivity's no-context case now asserts ev.HttpRequest.URL == "" (NOT s.Category) and passes.
    - The two pre-existing ContextJSON tests are updated to the first-class assertions and pass.
    - `cd C:\Users\Drupad\argus-sdk && go test ./internal/ocsf/... -count=1` passes (all old + new tests).
  </acceptance_criteria>
  <verify>
    <automated>cd C:\Users\Drupad\argus-sdk && go build ./... && go vet ./internal/ocsf/... && go test ./internal/ocsf/... -count=1 2>&1</automated>
  </verify>
  <done>web_resources + databucket are first-class on Event and round-trip; HTTP URL is honest (no category mislabel); TestMap_HTTPActivity updated for the empty-URL no-context case; no Unmapped string placeholders remain for these; full ocsf suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| signal.ContextJSON (operator/instrumentation-supplied) → OCSF Event fields | context.url / resource_url / vector_index flow into the OCSF wire format consumed by downstream SIEMs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering (injection) | context-derived URL/databucket into OCSF JSON | mitigate | Values are placed in struct fields and serialized via encoding/json (existing Map path) — they are data, never JSON syntax; no string concatenation of context values into the wire format |
| T-07-02 | Repudiation | URL mislabeled as category (404 fidelity) | mitigate | http_request.url derived only from real context URL fields or left empty — never silently substituted with s.Category |
| T-07-03 | Repudiation | non-deterministic logged_time blocks golden tests | mitigate | Injectable clock; deterministic output under a fixed clock |
| T-07-SC | Tampering | package installs | accept | No new dependencies — uses stdlib encoding/json + time only |
</threat_model>

<verification>
cd C:\Users\Drupad\argus-sdk && go build ./... && go vet ./internal/ocsf/... && go test ./internal/ocsf/... -count=1
</verification>

<success_criteria>
- Mapper clock injectable; fixed-clock Map is deterministic; default constructor unchanged (R-71, SC-1).
- web_resources + databucket first-class on Event; populated for 6001/6005 from context; omitted when absent (R-71, SC-1).
- HTTP url derived from context or omitted — no s.Category placeholder at the former mapper.go:396; TestMap_HTTPActivity updated to assert the empty-URL no-context case (R-71, SC-1).
- All affected classes round-trip; no pre-existing mapper test regresses; F17 closed (no longer deferred).
- Zero new dependencies; go build/vet/test clean for internal/ocsf.
</success_criteria>

<output>
Create .planning/workstreams/ws-c-v1-release/phases/07-release-hardening/07-01-SUMMARY.md when done
</output>
</content>
