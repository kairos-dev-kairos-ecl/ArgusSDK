---
phase: 07-release-hardening
plan: 03
type: execute
wave: 2
depends_on: ["07-02"]   # ADVISORY 4: freeze the EUC config key names 07-02 introduces before this sample documents them; file-disjoint, this only orders them
files_modified:
  - config/agent.example.yaml
  - deploy/agent.yaml
  - deploy/kubernetes/deployment.yaml
  - README.md
  - Makefile
  - .planning/WORKSTREAMS.md
  - internal/buffer/buffer.go
autonomous: true
requirements:
  - R-73   # docs/config consolidation: single canonical sample; buffer keys actually decode
  - R-74   # README synced to go.mod reality + observability/deploy/xdr/EUC matrix
  - R-75   # Makefile docker builds Dockerfile; install targets ./cmd/argus-agent
  - R-77   # milestone record: .planning/WORKSTREAMS.md ties WS-A/B/C for v1.0
must_haves:
  truths:
    - "config/agent.example.yaml is the SINGLE canonical sample and contains every current config section: agent (incl. xdr_endpoint), auth, ingest, buffer, outputs, tls, observability, logging, and the EUC watch list (AIEndpoints + LocalInferencePorts)"
    - "The sample's buffer keys are the keys that buffer.Config ACTUALLY decodes — buffer.Config carries snake_case mapstructure tags so dir / max_size_mb / flush_interval / drain_on_reconnect / backoff_* all bind (previously only dir bound; the bogus max_segment_bytes/max_total_bytes keys silently dropped)"
    - "deploy/agent.yaml no longer duplicates the full config — it is either deleted or reduced to a thin note that points at config/agent.example.yaml; the k8s ConfigMap does not drift from the canonical example"
    - "README tech-stack table lists only modules actually in go.mod — no github.com/oklog/ulid and no github.com/prometheus/client_golang"
    - "README directory structure includes connector/elastic, connector/factory, the observability server, and test/llmsignal"
    - "README documents the observability endpoints (/healthz, /readyz, /metrics), the deploy artifacts (Dockerfile + k8s manifest), agent.xdr_endpoint, and the EUC platform support matrix incl. the macOS Network Extension deferral; the Quick Start path is valid"
    - "Makefile docker target builds the existing Dockerfile (no TODO echo); Makefile install target targets ./cmd/argus-agent (not ./cmd/argus)"
    - ".planning/WORKSTREAMS.md exists, lists WS-A/WS-B/WS-C with their phases and v1.0 status, satisfying the Makefile G5 reference"
  artifacts:
    - path: "config/agent.example.yaml"
      provides: "Canonical sample with all sections incl. observability, agent.xdr_endpoint, EUC endpoints/ports; buffer keys use the REAL decoding names (max_size_mb, flush_interval, ...)"
      contains: "observability"
    - path: "internal/buffer/buffer.go"
      provides: "buffer.Config with snake_case mapstructure tags so the canonical sample's buffer keys actually decode (dir/max_size_mb/flush_interval/drain_on_reconnect/backoff_*)"
      contains: "mapstructure"
    - path: "README.md"
      provides: "Synced tech stack, structure, observability/deploy/xdr_endpoint/EUC matrix, valid quick start"
      contains: "/healthz"
    - path: "Makefile"
      provides: "docker builds Dockerfile; install targets ./cmd/argus-agent"
      contains: "./cmd/argus-agent"
    - path: ".planning/WORKSTREAMS.md"
      provides: "Milestone-level v1.0 record across WS-A/WS-B/WS-C"
      contains: "WS-C"
  key_links:
    - from: "config/agent.example.yaml buffer section"
      to: "internal/buffer/buffer.go Config (mapstructure tags)"
      via: "snake_case keys (max_size_mb, flush_interval, ...) decode into buffer.Config"
      pattern: "mapstructure"
    - from: "deploy/agent.yaml + deploy/kubernetes/deployment.yaml"
      to: "config/agent.example.yaml"
      via: "reference the canonical sample; no full duplication / no drift"
      pattern: "agent.example.yaml"
    - from: "Makefile docker target"
      to: "Dockerfile"
      via: "docker build -f Dockerfile"
      pattern: "docker build"
---

<objective>
Make the repo honest about its own state and remove duplication, satisfying ROADMAP Phase 7 Success Criteria 3, 4, 5, 7 (R-73 docs/config consolidation, R-74 README sync, R-75 Makefile, R-77 milestone record) per the 07-CONTEXT LOCKED decisions. This is primarily a docs/config/build cleanup plan; the ONLY production Go change is adding mapstructure tags to buffer.Config so the canonical sample's buffer keys actually decode (the honest-state fix — see below).

LOCKED items:
- Sample config consolidation (R-73): config/agent.example.yaml is the SINGLE canonical sample — add every current field (observability, agent.xdr_endpoint, the EUC watch list AIEndpoints/LocalInferencePorts, outputs, tls, buffer, logging). Resolve the deploy/agent.yaml duplication (delete-and-reference OR reduce to a thin k8s note pointing at the canonical file — Claude's discretion). The k8s ConfigMap (deploy/kubernetes/deployment.yaml) may keep an inline config but must NOT drift from the canonical example.
- Honest buffer keys (R-73, ADVISORY 3): internal/buffer/buffer.go Config has NO mapstructure tags today, so viper only binds the field whose lowercased name matches the YAML key (dir). The sample's previously-documented `buffer.max_segment_bytes` / `max_total_bytes` keys DO NOT EXIST on buffer.Config and silently fail to decode — exactly the "repo dishonest about its state" problem this phase fixes. RESOLVE it: add snake_case mapstructure tags to every buffer.Config field (Dir→`mapstructure:"dir"`, MaxSizeMB→`mapstructure:"max_size_mb"`, FlushInterval→`mapstructure:"flush_interval"`, DrainOnReconnect→`mapstructure:"drain_on_reconnect"`, BackoffBase→`mapstructure:"backoff_base"`, BackoffMax→`mapstructure:"backoff_max"`, BackoffJitter→`mapstructure:"backoff_jitter"`), and make the canonical sample (and the k8s ConfigMap) use these REAL decoding keys — drop the bogus max_segment_bytes / max_total_bytes. The previously-dead buffer sizing is now wired; record that in the SUMMARY.
- README sync (R-74): remove github.com/oklog/ulid/v2 and github.com/prometheus/client_golang from the tech stack (they are NOT in go.mod — BatchID uses crypto/rand; metrics are hand-rolled). List only deps actually present. Add connector/elastic, connector/factory, the observability server, and test/llmsignal to the directory structure. Document /healthz, /readyz, /metrics; the deploy artifacts (Dockerfile, k8s manifest); agent.xdr_endpoint; and the EUC platform support matrix incl. the macOS Network Extension deferral. Ensure the Quick Start path is valid against the consolidated config.
- Makefile (R-75): docker target builds the existing Dockerfile (replace the TODO echo); install target points at ./cmd/argus-agent (currently ./cmd/argus — a real bug).
- Milestone record (R-77): create .planning/WORKSTREAMS.md tying WS-A/WS-B/WS-C for v1.0 — list the three workstreams, their phases, and v1.0 status. This ALSO satisfies the Makefile header reference to .planning/WORKSTREAMS.md G5.

Constraints (binding):
- The ONLY *.go file this plan may touch is internal/buffer/buffer.go (mapstructure tags only — no logic change). Everything else is docs/config/build. File-disjoint from 07-01 (ocsf) and 07-04 (ci); file-disjoint from 07-02 too, but ordered AFTER 07-02 (depends_on, wave 2) so the EUC config key names 07-02 introduces are frozen before this sample documents them (ADVISORY 4).
- Do NOT modify any workstream ROADMAP.md and do NOT modify the top-level .planning/STATE.md. The milestone deliverable here is .planning/WORKSTREAMS.md (the 07-CONTEXT alternative).
- agent.xdr_endpoint is the Phase-5 AgentConfig field (mode: remote XDR registration endpoint). Document/sample it as such; do not invent unrelated fields.
- EUC watch list keys in the sample must match what 07-02 reads from Config (AIEndpoints + LocalInferencePorts under the agent's EUC config section). Because this plan depends_on 07-02, those key names are fixed by the time this runs — mirror them exactly.

Purpose: a v1.0 cut must not advertise dependencies it does not have, duplicate config three ways, ship config keys that silently do not decode, or ship a broken `make install`. The milestone record is the single index a reader uses to understand the whole release.
Output: one canonical sample whose buffer keys actually decode, de-duplicated deploy artifacts, a truthful README, a correct Makefile, and a v1.0 WORKSTREAMS index.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/ws-c-v1-release/ROADMAP.md
@.planning/workstreams/ws-c-v1-release/phases/07-release-hardening/07-CONTEXT.md
@config/agent.example.yaml
@deploy/agent.yaml
@deploy/kubernetes/deployment.yaml
@Dockerfile
@README.md
@Makefile
@go.mod
@internal/agent/agent.go
@internal/agent/observability.go
@internal/buffer/buffer.go
@internal/collector/euc/euc.go

<interfaces>
Facts the executor must reflect accurately (verified 2026-06-12):

go.mod direct deps (the ONLY ones the README tech stack may list as direct):
  spf13/cobra, spf13/viper, go.uber.org/zap, google.golang.org/grpc, google.golang.org/protobuf,
  fsnotify/fsnotify, mitchellh/mapstructure, twmb/franz-go (Kafka), gopkg.in/yaml.v3,
  testcontainers/testcontainers-go (+ kafka/elasticsearch modules — TEST ONLY).
  NOT present: github.com/oklog/ulid/v2, github.com/prometheus/client_golang. REMOVE both from README.

buffer.Config (internal/buffer/buffer.go :47-73) — fields and the REAL decoding keys after this plan adds mapstructure tags:
  Dir              → mapstructure:"dir"
  MaxSizeMB        → mapstructure:"max_size_mb"          # total disk cap in MB; default 256
  FlushInterval    → mapstructure:"flush_interval"        # drain attempt interval; default 5s
  DrainOnReconnect → mapstructure:"drain_on_reconnect"    # default true
  BackoffBase      → mapstructure:"backoff_base"          # default 2s
  BackoffMax       → mapstructure:"backoff_max"           # default 5m
  BackoffJitter    → mapstructure:"backoff_jitter"        # default 30s
  IMPORTANT: there is NO max_segment_bytes / max_total_bytes field — those keys never decoded. The sample and k8s ConfigMap must use the real keys above.

Config sections that MUST appear in config/agent.example.yaml (from internal/agent/agent.go Config + observability.go):
  agent: { group_id, instance_name, mode, xdr_endpoint }   # xdr_endpoint = Phase-5 remote registration endpoint
  auth: { install_token, instance_id, credential }
  ingest: { listen: { grpc, unix } }
  buffer: { dir, max_size_mb, flush_interval, drain_on_reconnect, backoff_base, backoff_max, backoff_jitter }  # the real mapstructure keys this plan adds
  outputs: [ { name, type, endpoint, ocsf, tls{enabled,min_version,ca_cert}, auth{...} } ]   # argusxdr + a Mode-2 example
  tls: { enabled, min_version, ca_cert }
  observability: { disabled, addr }   # default addr 127.0.0.1:9090 (observability.go:19)
  logging: { level, format }
  EUC watch list: AIEndpoints []string + LocalInferencePorts []int  # under the section 07-02 added to Config; match those keys (depends_on 07-02)

Observability endpoints (observability.go): /healthz (liveness), /readyz (readiness), /metrics. Default bind 127.0.0.1:9090.

Deploy artifacts that exist and must be documented: Dockerfile (distroless static, EXPOSE 9090 5002, entrypoint --config /etc/argus-agent/agent.yaml), deploy/kubernetes/deployment.yaml (ConfigMap + Deployment with liveness/readiness probes on /healthz//readyz).

Directory entries to ADD to README structure (they exist): internal/connector/elastic/, internal/connector/factory/, the observability server (internal/agent/observability.go), test/llmsignal/. (internal/connector/syslog already documented; argusxdr/kafka/splunk already documented.)

EUC platform matrix (from euc package + 07-CONTEXT deferred): Linux eBPF, Windows ETW/WFP, macOS Network Extension — note macOS full Network Extension PACKAGING is deferred post-Phase-6 (tracked, not shipped in v1.0).

Makefile current bugs (Makefile :45-51): `docker` echoes a TODO; `install` runs `go install ./cmd/argus` (wrong — the binary is ./cmd/argus-agent). Header (Makefile:2) references .planning/WORKSTREAMS.md G5 which does not yet exist.

Workstreams for the milestone record (from .planning/workstreams/): ws-a-* (proto foundation), ws-b-connector-layer (connector layer + agent wiring; phases incl. 03-review-remediation, 04-agent-wiring), ws-c-v1-release (phases 5 XDR Mode-1, 6 EUC OS collectors, 7 release hardening). Read .planning/workstreams/ to enumerate the actual phase dirs before writing the index.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire buffer.Config mapstructure tags + consolidate the canonical sample config + de-duplicate deploy artifacts</name>
  <files>internal/buffer/buffer.go, config/agent.example.yaml, deploy/agent.yaml, deploy/kubernetes/deployment.yaml</files>
  <read_first>
    - internal/buffer/buffer.go (Config :47-73 — fields have NO mapstructure tags today; only `dir` binds)
    - config/agent.example.yaml (current sample to extend)
    - deploy/agent.yaml (the duplicate to delete-or-reduce)
    - deploy/kubernetes/deployment.yaml (inline ConfigMap that must not drift)
    - internal/agent/agent.go (Config :27-86 — authoritative field list)
    - internal/agent/observability.go (ObservabilityConfig :21-30, default addr :19)
    - internal/collector/euc/euc.go (Config :27-42 — EUC watch list keys; match 07-02's Config additions)
  </read_first>
  <action>
    buffer.Config tags (the honest-state fix): add snake_case mapstructure tags to every field of buffer.Config (internal/buffer/buffer.go :47-73) so viper decodes them: Dir→`mapstructure:"dir"`, MaxSizeMB→`mapstructure:"max_size_mb"`, FlushInterval→`mapstructure:"flush_interval"`, DrainOnReconnect→`mapstructure:"drain_on_reconnect"`, BackoffBase→`mapstructure:"backoff_base"`, BackoffMax→`mapstructure:"backoff_max"`, BackoffJitter→`mapstructure:"backoff_jitter"`. Tags only — do NOT change defaults, logic, or DefaultConfig(). This is the sole .go edit in this plan.
    Make config/agent.example.yaml the single canonical sample covering EVERY current section: agent (group_id, instance_name, mode, xdr_endpoint), auth, ingest.listen (grpc + unix), buffer (use the REAL decoding keys: dir, max_size_mb, flush_interval, drain_on_reconnect, backoff_base, backoff_max, backoff_jitter — DO NOT use max_segment_bytes / max_total_bytes; those never decoded), outputs (one argusxdr Mode-1 example + one commented/active Mode-2 example with ocsf:true), tls (enabled/min_version/ca_cert), observability (disabled + addr, note default 127.0.0.1:9090 and the 0.0.0.0 k8s note), logging (level + format), and the EUC watch list (AIEndpoints with a couple of example AI hostnames + LocalInferencePorts 11434/1234, with a comment that this list is hot-reloadable via SIGHUP — delivered by 07-02). Each section keeps a one-line comment. Keep the env-override note at the top.
    Resolve deploy/agent.yaml duplication: reduce it to a thin, clearly-labelled pointer file (a short header comment stating that config/agent.example.yaml is the canonical sample and this directory's k8s manifest carries the deployment-specific ConfigMap) OR delete it entirely and rely on config/agent.example.yaml + the k8s manifest (Claude's discretion — pick one and state it in the SUMMARY). If reduced, it must NOT contain a second full config body.
    Reconcile deploy/kubernetes/deployment.yaml's inline ConfigMap so it does not drift from the canonical example: it may stay inline (k8s needs a self-contained manifest) but its agent.yaml block must use the same field names/structure as the canonical sample (add observability/xdr_endpoint/EUC keys consistently AND the real buffer keys; values may differ for the cluster context). Do not remove the k8s-specific bits (0.0.0.0 binds, probes, secret env refs).
  </action>
  <acceptance_criteria>
    - buffer.Config carries snake_case mapstructure tags on all fields (dir, max_size_mb, flush_interval, drain_on_reconnect, backoff_base, backoff_max, backoff_jitter); defaults/logic unchanged; `go build ./internal/buffer/...` clean.
    - A sample buffer key actually decodes: a tiny viper/mapstructure decode assertion (or a buffer-config test) proves e.g. `max_size_mb: 512` from YAML lands as buffer.Config.MaxSizeMB == 512 (previously it would silently stay at the default).
    - config/agent.example.yaml contains all of: agent.xdr_endpoint, observability, AIEndpoints, LocalInferencePorts, plus auth/ingest/buffer/outputs/tls/logging; the buffer section uses max_size_mb / flush_interval (NOT max_segment_bytes / max_total_bytes).
    - deploy/agent.yaml no longer holds a full duplicate config (deleted, or a thin pointer with no second config body).
    - deploy/kubernetes/deployment.yaml ConfigMap field structure matches the canonical sample (observability + xdr_endpoint + real buffer keys present; no contradictory keys).
    - `cd C:\Users\Drupad\argus-sdk && grep -c "observability" config/agent.example.yaml` is >= 1; `grep -c "AIEndpoints" config/agent.example.yaml` is >= 1; `grep -c "xdr_endpoint" config/agent.example.yaml` is >= 1; `grep -c "max_size_mb" config/agent.example.yaml` is >= 1; `grep -c "max_segment_bytes" config/agent.example.yaml` is 0.
  </acceptance_criteria>
  <verify>
    <automated>cd C:\Users\Drupad\argus-sdk && go build ./internal/buffer/... && go test ./internal/buffer/... -count=1 -run "Config|Decode|Mapstructure" 2>&1</automated>
  </verify>
  <done>buffer.Config has snake_case mapstructure tags so the sample's buffer keys decode (previously dead sizing now wired); one canonical sample with all sections using the real buffer keys; deploy/agent.yaml de-duplicated; k8s ConfigMap aligned, no drift.</done>
</task>

<task type="auto">
  <name>Task 2: Sync README to reality (tech stack, structure, observability/deploy/xdr/EUC, quick start)</name>
  <files>README.md</files>
  <read_first>
    - README.md (current — tech stack :134-145, structure :104-130, EUC :60-77, quick start :79-91)
    - go.mod (authoritative dep list)
    - internal/agent/observability.go (endpoint names)
    - Dockerfile + deploy/kubernetes/deployment.yaml (deploy artifacts to document)
    - config/agent.example.yaml (updated in Task 1 — quick start must match it, incl. the real buffer keys)
  </read_first>
  <action>
    Edit README.md so every claim is true:
    - Technology Stack table: REMOVE the `github.com/oklog/ulid/v2` (Signal IDs) and `github.com/prometheus/client_golang` (Metrics) rows. Replace Signal IDs with "crypto/rand (stdlib)" and Metrics with "hand-rolled (stdlib net/http /metrics)". Add rows for the deps that ARE direct and notable: fsnotify (config watch), mapstructure (config decode), twmb/franz-go (Kafka), testcontainers-go (integration tests, test-only). Keep cobra/viper/zap/grpc/protobuf.
    - Directory Structure: add internal/connector/elastic/ (Mode 2: OCSF → Elasticsearch _bulk), internal/connector/factory/ (connector construction), the observability server (note internal/agent/observability.go — /healthz//readyz//metrics), and test/llmsignal/ (local LLM signal suite). Keep existing entries accurate.
    - Add an "Observability" subsection documenting /healthz (liveness), /readyz (readiness, gated until startup completes), /metrics (dispatcher counters), default bind 127.0.0.1:9090 and the 0.0.0.0 container note.
    - Add a "Deployment" subsection documenting the Dockerfile (distroless static, ports 9090/5002, --config /etc/argus-agent/agent.yaml) and deploy/kubernetes/deployment.yaml (ConfigMap + Deployment with liveness/readiness probes).
    - Document agent.xdr_endpoint (the Phase-5 remote XDR registration endpoint used in mode: remote) where the auth/mode model is described.
    - EUC section: present a platform support matrix — Linux (eBPF), Windows (ETW/WFP), macOS (Network Extension) — and explicitly note that the macOS full Network Extension PACKAGING is deferred post-v1.0 (tracked from Phase 6). Keep the "what it does NOT do" list.
    - Quick Start: remove the "(stub — implementation in progress)" label; ensure the steps are valid against the consolidated config (cp config/agent.example.yaml agent.yaml; set group_id/install_token/outputs.endpoint; run argus-agent --config agent.yaml). Keep the go install line pointing at ./cmd/argus-agent.
  </action>
  <acceptance_criteria>
    - `cd C:\Users\Drupad\argus-sdk && grep -c "oklog/ulid" README.md` is 0 and `grep -c "prometheus/client_golang" README.md` is 0.
    - `cd C:\Users\Drupad\argus-sdk && grep -c "/healthz" README.md` is >= 1; `grep -c "/readyz" README.md` >= 1; `grep -c "/metrics" README.md` >= 1.
    - `cd C:\Users\Drupad\argus-sdk && grep -c "connector/elastic" README.md` >= 1; `grep -c "connector/factory" README.md` >= 1; `grep -c "test/llmsignal" README.md` >= 1.
    - `cd C:\Users\Drupad\argus-sdk && grep -c "xdr_endpoint" README.md` >= 1; `grep -ic "network extension" README.md` >= 1.
    - The "(stub — implementation in progress)" Quick Start label is removed.
  </acceptance_criteria>
  <verify>
    <automated>cd C:\Users\Drupad\argus-sdk && grep -c "oklog/ulid" README.md ; grep -c "prometheus/client_golang" README.md ; grep -c "/healthz" README.md ; grep -c "connector/elastic" README.md ; grep -c "xdr_endpoint" README.md</automated>
  </verify>
  <done>README lists only real deps, documents observability/deploy/xdr_endpoint/EUC matrix, structure includes elastic/factory/observability/llmsignal, and Quick Start is valid.</done>
</task>

<task type="auto">
  <name>Task 3: Fix Makefile (docker + install) and create the v1.0 WORKSTREAMS milestone record</name>
  <files>Makefile, .planning/WORKSTREAMS.md</files>
  <read_first>
    - Makefile (docker :45-47, install :49-51, header :1-2)
    - Dockerfile (build args — VERSION arg available)
    - .planning/workstreams/ (enumerate the actual workstream + phase directories)
    - .planning/workstreams/ws-c-v1-release/ROADMAP.md (phase list + statuses)
  </read_first>
  <action>
    Makefile:
    - docker target: replace the TODO echo with a real build of the existing Dockerfile, e.g. `docker build -t argus-agent:$(VERSION) -f Dockerfile .` with a `VERSION ?= dev` variable near the top so the tag is overridable and the Dockerfile's ARG VERSION can be passed via `--build-arg VERSION=$(VERSION)`. Keep it a single, runnable target.
    - install target: change `go install ./cmd/argus` to `go install ./cmd/argus-agent`.
    - Leave proto/build/build-all/test/test-int/test-llm/lint targets unchanged.
    .planning/WORKSTREAMS.md (create): a milestone-level v1.0 index. Enumerate the real workstream dirs under .planning/workstreams/ first, then write a table/section listing each workstream (WS-A proto foundation, WS-B connector layer + agent wiring, WS-C v1.0 release hardening), its phases (with numbers + short titles taken from the actual phase dirs / ROADMAPs), and a v1.0 status column (e.g. complete / in-progress). Include a top-line statement that v1.0 = XDR Mode-1 + EUC, fully implemented and verified, and that this file is the milestone index the Makefile header references (G5). Do NOT modify any ROADMAP.md or STATE.md.
  </action>
  <acceptance_criteria>
    - `cd C:\Users\Drupad\argus-sdk && grep -c "docker build" Makefile` is >= 1 and `grep -c "TODO" Makefile` is 0 (the docker TODO echo is gone).
    - `cd C:\Users\Drupad\argus-sdk && grep -c "go install ./cmd/argus-agent" Makefile` is >= 1 and `grep -c "go install ./cmd/argus$" Makefile` is 0 (no install of the wrong path).
    - .planning/WORKSTREAMS.md exists and `grep -c "WS-C" .planning/WORKSTREAMS.md` is >= 1 and it lists WS-A and WS-B too.
    - No ROADMAP.md or .planning/STATE.md file is modified by this task (verify via git status — only Makefile and WORKSTREAMS.md changed by this task's files).
  </acceptance_criteria>
  <verify>
    <automated>cd C:\Users\Drupad\argus-sdk && grep -c "docker build" Makefile && grep -c "go install ./cmd/argus-agent" Makefile && test -f .planning/WORKSTREAMS.md && grep -c "WS-C" .planning/WORKSTREAMS.md</automated>
  </verify>
  <done>Makefile docker builds the Dockerfile; install targets ./cmd/argus-agent; .planning/WORKSTREAMS.md indexes WS-A/B/C for v1.0; the Makefile G5 reference is satisfied; no ROADMAP/STATE modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Published docs/sample config → operator deployment decisions | A misleading README/sample causes operators to deploy with wrong assumptions (e.g. binding metrics publicly, missing TLS, or buffer keys that silently do not apply) |
| Makefile docker target → image build | The docker target invokes a local Docker build of the repo Dockerfile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Repudiation / Information | README advertises deps not present (supply-chain confusion) | mitigate | Tech stack reconciled to go.mod exactly; ulid/prometheus removed |
| T-07-09 | Information Disclosure | sample binds observability publicly by default | mitigate | Canonical sample documents 127.0.0.1:9090 default and flags 0.0.0.0 as the explicit container-only opt-in |
| T-07-10 | Tampering | config drift between three places leads to insecure deploy | mitigate | Single canonical sample; deploy/agent.yaml de-duplicated; k8s ConfigMap structurally aligned |
| T-07-11 | Repudiation | sample buffer keys silently do not decode (sizing not enforced) | mitigate | buffer.Config gets snake_case mapstructure tags; sample uses the real keys; decode assertion proves a sample key binds |
| T-07-SC | Tampering | package installs | accept | Docs/config/build + one tags-only buffer.go edit — no new dependency; docker target builds the existing pinned Dockerfile |
</threat_model>

<verification>
cd C:\Users\Drupad\argus-sdk && go build ./...
cd C:\Users\Drupad\argus-sdk && go test ./internal/buffer/... -count=1
cd C:\Users\Drupad\argus-sdk && grep -c "oklog/ulid" README.md ; grep -c "prometheus/client_golang" README.md
cd C:\Users\Drupad\argus-sdk && grep -c "observability" config/agent.example.yaml ; grep -c "AIEndpoints" config/agent.example.yaml ; grep -c "max_size_mb" config/agent.example.yaml ; grep -c "max_segment_bytes" config/agent.example.yaml
cd C:\Users\Drupad\argus-sdk && grep -c "docker build" Makefile ; grep -c "go install ./cmd/argus-agent" Makefile
cd C:\Users\Drupad\argus-sdk && test -f .planning/WORKSTREAMS.md && grep -c "WS-C" .planning/WORKSTREAMS.md
# go build confirms the buffer.go tags-only edit compiles and nothing else broke.
</verification>

<success_criteria>
- buffer.Config carries snake_case mapstructure tags so the canonical sample's buffer keys actually decode; previously-dead buffer sizing is now wired (R-73, SC-3).
- config/agent.example.yaml is the single canonical sample with every section incl. observability/xdr_endpoint/EUC and the real buffer keys; deploy/agent.yaml de-duplicated; k8s ConfigMap aligned (R-73, SC-3).
- README tech stack matches go.mod (no ulid/prometheus); structure adds elastic/factory/observability/llmsignal; observability/deploy/xdr_endpoint/EUC matrix documented; Quick Start valid (R-74, SC-4).
- Makefile docker builds the Dockerfile; install targets ./cmd/argus-agent (R-75, SC-5).
- .planning/WORKSTREAMS.md ties WS-A/WS-B/WS-C for v1.0 and satisfies the Makefile G5 reference (R-77, SC-7).
- Only buffer.go changed among *.go files (tags only); go build clean; no ROADMAP.md or STATE.md modified.
</success_criteria>

<output>
Create .planning/workstreams/ws-c-v1-release/phases/07-release-hardening/07-03-SUMMARY.md when done.
The SUMMARY must note that the previously-dead buffer sizing keys (buffer.Config had no mapstructure tags; only `dir` bound) are now wired via snake_case tags, and the canonical sample/k8s ConfigMap use the real decoding keys (max_size_mb/flush_interval/...) instead of the bogus max_segment_bytes/max_total_bytes.
</output>
</content>
