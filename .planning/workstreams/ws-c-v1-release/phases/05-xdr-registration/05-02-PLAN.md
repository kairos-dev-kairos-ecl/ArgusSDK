---
phase: 05-xdr-registration
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - internal/auth/state.go
  - internal/auth/state_test.go
  - internal/agent/registration.go
  - internal/agent/registration_test.go
  - internal/agent/agent.go
  - internal/agent/agent_test.go
  - config/agent.example.yaml
autonomous: true
requirements: [R-52, R-54, R-55]
must_haves:
  truths:
    - "InstanceID + Credential are persisted to agent-state.json encrypted via secrets.Store (AES-256-GCM) and never written in plaintext"
    - "On start, a persisted InstanceID is loaded and reused; registration is skipped (idempotent restart)"
    - "Credential refresh replaces the stored credential in agent-state.json via secrets.Store (write-then-rename: atomic on POSIX, best-effort on Windows)"
    - "AgentConfig has an explicit xdr_endpoint field (the XDR base URL for registration + refresh); resolveIdentity reads cfg.Agent.XDREndpoint and passes it to auth.NewRemoteRegistrar; mode: remote with an empty xdr_endpoint returns a clear config error"
    - "agent.ensureInstance uses the real remote registrar by default in mode: remote; mode: local keeps the existing localRegistrar; a pre-set Auth.InstanceID short-circuits registration in both modes"
    - "Agent.RefreshCredential(ctx) is a reachable runtime entry point: it builds the remote CredentialRefresher from cfg.Agent.XDREndpoint, refreshes with the current Identity, and replaces the stored credential via the secrets store"
    - "mode: local resolves identity WITHOUT requiring ARGUS_MASTER_KEY or a secrets store; mode: remote requires ARGUS_MASTER_KEY (or an injected key) as a startup precondition"
    - "After a successful remote registration the InstallToken is cleared/ignored and the resolved InstanceID is persisted before the agent proceeds"
    - "An install-token reuse error from the registrar is surfaced as a typed error and must NOT overwrite a good persisted InstanceID"
  artifacts:
    - path: "internal/auth/state.go"
      provides: "encrypted load/save of Identity (InstanceID+Credential) to agent-state.json via secrets.Store, with credential replace (atomic on POSIX, best-effort on Windows)"
      contains: "func LoadIdentity"
    - path: "internal/agent/agent.go"
      provides: "AgentConfig.XDREndpoint field; resolveIdentity (persisted-state-first, mode-aware, master-key gating); Agent.RefreshCredential reachable entry point"
      contains: "XDREndpoint"
    - path: "internal/agent/registration.go"
      provides: "mode-aware registrar selection: remote adapter wrapping auth.remoteRegistrar for mode:remote, localRegistrar retained for mode:local"
      contains: "remote"
    - path: "internal/agent/agent_test.go"
      provides: "agent-package httptest mock XDR (own, not 05-01's); register→persist→reload-across-restart, refresh-via-RefreshCredential, install-token single-use, remote-vs-local selection, xdr_endpoint config-error, and local-mode-without-master-key tests"
    - path: "config/agent.example.yaml"
      provides: "documented agent.xdr_endpoint field with a comment"
  key_links:
    - from: "internal/auth/state.go"
      to: "secrets.Store.SaveSecrets / LoadSecrets"
      via: "Identity fields stored under fixed keys in the encrypted agent-state.json map"
      pattern: "(SaveSecrets|LoadSecrets)"
    - from: "internal/agent/agent.go"
      to: "auth.NewRemoteRegistrar"
      via: "resolveIdentity builds the remote registrar from cfg.Agent.XDREndpoint in mode: remote"
      pattern: "NewRemoteRegistrar"
    - from: "internal/agent/agent.go"
      to: "auth.NewRemoteCredentialRefresher"
      via: "Agent.RefreshCredential builds the refresher from cfg.Agent.XDREndpoint"
      pattern: "NewRemoteCredentialRefresher"
    - from: "internal/agent/agent.go"
      to: "auth.LoadIdentity / auth.SaveIdentity"
      via: "resolveIdentity loads persisted identity first, then persists a freshly registered one"
      pattern: "(LoadIdentity|SaveIdentity)"
---

<objective>
Persist and wire the real registration lifecycle, add the missing runtime config + entry points the
plan-checker flagged, and prove the whole flow against a mock XDR.

Add internal/auth/state.go: encrypted load/save of the Identity (InstanceID + Credential) to
agent-state.json via secrets.Store (AES-256-GCM, never plaintext), plus a credential-replace path for
refresh (write-then-rename: atomic on POSIX, best-effort on Windows — do not claim hard Windows
atomicity).

Add an explicit `XDREndpoint` field to AgentConfig (the XDR base URL used for registration + credential
refresh, e.g. "https://xdr.company.com:8080"). resolveIdentity reads cfg.Agent.XDREndpoint and passes
it to auth.NewRemoteRegistrar; mode: remote with an empty xdr_endpoint returns a clear config error. Do
NOT scrape the argusxdr OutputConfig.Endpoint. Document the field in config/agent.example.yaml.

Make internal/agent/registration.go mode-aware: mode: remote uses an adapter over
auth.NewRemoteRegistrar (the default), mode: local keeps the existing localRegistrar; a pre-set
Auth.InstanceID short-circuits in both modes. Update agent.start() to resolve identity
persisted-state-first (reuse a stored InstanceID and skip registration), then persist any freshly
registered InstanceID+Credential and clear the consumed InstallToken, never overwriting a good
persisted InstanceID when the registrar reports a consumed token. mode: local must resolve WITHOUT
requiring ARGUS_MASTER_KEY or a store; mode: remote requires the master key (it persists identity).

Add a reachable runtime entry point Agent.RefreshCredential(ctx) that builds the remote
CredentialRefresher from cfg.Agent.XDREndpoint, refreshes with the current Identity, and atomically
replaces the stored credential via the secrets store. Automatic refresh-before-expiry SCHEDULING
remains deferred to Phase 7 — but the call path is wired and reachable here.

Output: internal/auth/state.go + AgentConfig.XDREndpoint + mode-aware registration + persisted-state
wiring + Agent.RefreshCredential + agent.example.yaml doc + Docker-free mock-XDR-backed tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/ws-c-v1-release/phases/05-xdr-registration/05-CONTEXT.md
@.planning/workstreams/ws-c-v1-release/phases/05-xdr-registration/05-01-PLAN.md

<interfaces>
From internal/auth/auth.go (contract):
- type Identity struct { GroupID, InstanceID, Credential string }
- type RegistrationRequest struct { GroupID, InstanceName, InstallToken, AgentVersion, Hostname, Platform string }
- type RegistrationResponse struct { InstanceID, Credential string }
- type Registrar interface { Register(ctx, RegistrationRequest) (*RegistrationResponse, error) }       // auth-layer signature
- type CredentialRefresher interface { Refresh(ctx, Identity) (newCredential string, err error) }
- const StateFile = "agent-state.json"

From internal/auth/remote.go (built in 05-01 — consume, do NOT reimplement). Constructors return the
EXPORTED interface types (WARNING 5), so consume them directly with no type friction:
- func NewRemoteRegistrar(baseURL string, httpClient *http.Client) Registrar
- func NewRemoteCredentialRefresher(baseURL string, httpClient *http.Client) CredentialRefresher
- var ErrInstallTokenConsumed error   // errors.Is target for reused/invalid install token
  // CROSS-PACKAGE GOTCHA: 05-01's newMockXDR helper lives in internal/auth/*_test.go and is NOT
  // importable from internal/agent tests. Do NOT attempt to reuse it from agent_test.go — stand up
  // an OWN small httptest mock XDR inside the internal/agent test files (Task 4).

From internal/secrets/store.go (reuse — AES-256-GCM, write-then-rename):
- func NewStore(path string, masterKey []byte) (*Store, error)   // masterKey must be exactly 32 bytes; nil reads ARGUS_MASTER_KEY (base64 32 bytes); ERRORS if masterKey nil AND ARGUS_MASTER_KEY unset
- func (s *Store) LoadSecrets() (map[string]string, error)       // empty map (no error) when file absent
- func (s *Store) SaveSecrets(secrets map[string]string) error   // encrypts whole map; O_CREATE|O_WRONLY|O_TRUNC 0600 temp + rename + chmod 0600. Rename is atomic on POSIX, BEST-EFFORT on Windows (store.go:234) — do NOT claim hard Windows atomicity in the SUMMARY.
- func GenerateMasterKey() (string, error)                       // base64 32-byte key (test helper)
  // SaveSecrets persists the ENTIRE map; to update one value, Load → mutate → Save. The
  // write-then-rename inside SaveSecrets IS the credential-replace path — no separate logic needed.
  // PRECONDITION (WARNING 4): NewStore fails when no master key is available. Therefore only
  // mode: remote (which persists identity) requires ARGUS_MASTER_KEY; mode: local must NOT construct
  // a store and must start without the key.

From internal/agent/registration.go (current — to be made mode-aware):
- type Registrar interface { Register(ctx, installToken, groupID string) (string, error) }   // agent-seam signature (DISTINCT from auth.Registrar)
- type localRegistrar struct{}  // SHA-256(groupID:installToken)[:16] — KEEP for mode: local (locked decision)
- func ensureInstance(ctx, cfg *Config, r Registrar) (string, error)  // returns InstanceID; short-circuits when cfg.Auth.InstanceID set

From internal/agent/agent.go (current — confirmed by read):
- type Config struct { Agent AgentConfig; Auth AuthConfig; ... }
- type AgentConfig struct { GroupID string `mapstructure:"group_id"`; InstanceName string `mapstructure:"instance_name"`; Mode string `mapstructure:"mode"` }
  // BLOCKER 1: AgentConfig has NO endpoint field today. This plan ADDS `XDREndpoint string \`mapstructure:"xdr_endpoint"\``.
  // The argusxdr OutputConfig.Endpoint (cfg.Outputs[i].Endpoint) is the signal-submission endpoint and
  // MUST NOT be scraped for the registration base URL — registration uses the new explicit field.
- type AuthConfig struct { InstallToken, InstanceID, Credential string }
- AgentConfig.Mode is "local" | "remote"
- start() currently calls (agent.go:170-176): id, err := ensureInstance(ctx, a.cfg, &localRegistrar{}); a.instanceID = id
  // agent.go:170-176 is the single registration call site to change. ingestLoop reads a.instanceID +
  // cfg.Agent.GroupID; the rest of start()/stop() is unrelated to this plan — touch ONLY the
  // registration block plus the new AgentConfig field and the RefreshCredential method.

stdlib only (NO new module): encoding/json or reuse of the secrets map; errors, fmt, runtime (Platform),
os (Hostname), net/http (refresher client), net/http/httptest (agent tests only).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Encrypted Identity persistence (auth/state.go) — load, save, credential replace</name>
  <files>internal/auth/state.go, internal/auth/state_test.go</files>
  <read_first>internal/auth/auth.go, internal/secrets/store.go</read_first>
  <behavior>
    - LoadIdentity(store *secrets.Store) (Identity, bool, error): reads the encrypted map; returns
      (Identity{GroupID,InstanceID,Credential}, true, nil) when an InstanceID is present, and
      (zero, false, nil) when the state file is absent or has no InstanceID. A decryption/format error
      propagates as a non-nil error.
    - SaveIdentity(store *secrets.Store, id Identity) error: loads the current map, sets the GroupID /
      InstanceID / Credential keys, and SaveSecrets the whole map — so the file is always encrypted and
      written via the store's write-then-rename. Round-trip: SaveIdentity then LoadIdentity returns the
      same Identity.
    - ReplaceCredential(store *secrets.Store, newCredential string) error: loads, overwrites only the
      Credential key, and SaveSecrets — InstanceID/GroupID are preserved; the new credential is
      readable after reload.
    - The on-disk file never contains the InstanceID or Credential in plaintext (it is AES-256-GCM
      ciphertext); a test reads the raw bytes and asserts the values do NOT appear.
  </behavior>
  <action>
    Create internal/auth/state.go. Define fixed map keys as constants (e.g. keyGroupID="group_id",
    keyInstanceID="instance_id", keyCredential="credential"). Implement LoadIdentity: call
    store.LoadSecrets(); build an Identity from the three keys; ok := id.InstanceID != ""; return
    (id, ok, nil); propagate any LoadSecrets error. Implement SaveIdentity: m, err :=
    store.LoadSecrets() (start from existing so unrelated keys survive); set the three keys from the
    Identity (only set non-empty values so a partial save never blanks a field); return
    store.SaveSecrets(m). Implement ReplaceCredential: load the map, set m[keyCredential]=newCredential,
    SaveSecrets — this reuses the store's O_TRUNC-temp + rename + chmod 0600 path (no new atomic logic;
    note the rename is atomic on POSIX, best-effort on Windows). Document that StateFile (auth.go) is the
    conventional path the agent passes to NewStore. Write state_test.go BEFORE implementing, using
    secrets.GenerateMasterKey to make a 32-byte key and a t.TempDir()-based store path: (a) Load on an
    absent file returns ok=false, no error; (b) Save then Load round-trips the Identity; (c)
    ReplaceCredential changes only the credential and Load reflects it while InstanceID/GroupID are
    unchanged; (d) read the raw state file bytes and assert the InstanceID and Credential strings do NOT
    appear (plaintext-on-disk negative test). NO new module — encoding via the existing secrets gob map.
    Do NOT use -race.
  </action>
  <verify>
    <automated>cd /c/Users/Drupad/argus-sdk && go test ./internal/auth/... -run TestIdentity -count=1 && go vet ./internal/auth/...</automated>
  </verify>
  <acceptance_criteria>
    - Identity persistence tests pass Docker-free.
    - Round-trip test asserts Save→Load returns an equal Identity (InstanceID + Credential preserved).
    - ReplaceCredential test asserts the credential changes and InstanceID/GroupID are unchanged after reload.
    - Plaintext negative test asserts the raw agent-state.json bytes contain neither the InstanceID nor the Credential string.
    - `grep -v '^#' internal/auth/state.go | grep -c "SaveSecrets"` is non-zero (persistence via secrets.Store).
    - go.mod gains no new module.
  </acceptance_criteria>
  <done>Identity (InstanceID+Credential) persists encrypted to agent-state.json via secrets.Store and reloads across a fresh Store; credential replace reuses the store's write-then-rename; nothing is plaintext on disk — R-52 met.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mode-aware registrar selection (agent/registration.go)</name>
  <files>internal/agent/registration.go, internal/agent/registration_test.go</files>
  <read_first>internal/agent/registration.go, internal/agent/agent.go, internal/auth/auth.go, internal/auth/remote.go</read_first>
  <behavior>
    - A remoteRegistrarAdapter implements the agent-seam Registrar (Register(ctx, installToken, groupID)
      (string, error)) by wrapping an auth.Registrar: it builds a RegistrationRequest (GroupID,
      InstallToken, InstanceName, AgentVersion, Hostname=os.Hostname, Platform=runtime.GOOS) and calls
      auth.Registrar.Register, returning RegistrationResponse.InstanceID and propagating the Credential
      out-of-band so start() can persist it (e.g. the adapter records the last RegistrationResponse, or
      Register returns both via a richer result the adapter exposes).
    - A selectRegistrar(cfg, ...) helper returns the localRegistrar for cfg.Agent.Mode == "local"
      (and any non-"remote" mode) and the remote adapter for cfg.Agent.Mode == "remote" (the default
      for remote mode, locked decision).
    - ensureInstance behavior is unchanged: a pre-set cfg.Auth.InstanceID short-circuits (no Register
      call) in BOTH modes; empty InstanceID + set InstallToken calls the selected registrar; both empty
      errors.
    - An ErrInstallTokenConsumed from the remote registrar propagates out of ensureInstance unchanged
      (errors.Is still matches) so the caller can avoid overwriting persisted state.
  </behavior>
  <action>
    Extend internal/agent/registration.go. Keep localRegistrar exactly as-is (mode: local path, locked).
    Add a remoteRegistrarAdapter struct holding an auth.Registrar plus the agent identity fields it needs
    to build the RegistrationRequest (InstanceName, AgentVersion) and a field to capture the returned
    Credential (e.g. lastCredential string, or have the adapter expose a typed result). Implement its
    Register(ctx, installToken, groupID) by constructing auth.RegistrationRequest{GroupID:groupID,
    InstallToken:installToken, InstanceName:..., AgentVersion:..., Hostname: os.Hostname best-effort,
    Platform: runtime.GOOS}, calling the wrapped auth.Registrar.Register, and on success storing the
    Credential and returning resp.InstanceID; on error return ("", err) WITHOUT swallowing
    ErrInstallTokenConsumed (return it wrapped so errors.Is still matches, or return it directly). Add a
    constructor NewRemoteRegistrarAdapter(r auth.Registrar, instanceName, agentVersion string) and a
    selectRegistrar(cfg *Config, remote agent.Registrar) Registrar helper (mode=="remote" → remote,
    else localRegistrar) — keep selection pure so start() can inject the adapter built from
    auth.NewRemoteRegistrar. Update the registration.go doc comment: the remote registrar is no longer a
    deferred sub-item — it is the default for mode: remote; localRegistrar remains the mode: local path.
    Write/extend registration_test.go BEFORE implementing: (a) selectRegistrar returns localRegistrar for
    mode local and the adapter for mode remote; (b) the adapter, wrapping a fake auth.Registrar that
    returns a canned RegistrationResponse, yields that InstanceID through ensureInstance and exposes the
    Credential; (c) the adapter wrapping a fake auth.Registrar returning ErrInstallTokenConsumed makes
    ensureInstance return an error for which errors.Is(err, auth.ErrInstallTokenConsumed) is true;
    (d) a pre-set cfg.Auth.InstanceID short-circuits both modes (registrar Register is never called —
    assert via a call-count fake). NO new module. Do NOT use -race.
  </action>
  <verify>
    <automated>cd /c/Users/Drupad/argus-sdk && go test ./internal/agent/ -run "TestEnsureInstance|TestSelectRegistrar|TestRemoteAdapter" -count=1 && go vet ./internal/agent/...</automated>
  </verify>
  <acceptance_criteria>
    - Registration tests pass Docker-free.
    - selectRegistrar test asserts localRegistrar for mode "local" (and unset) and the remote adapter for mode "remote".
    - Adapter test asserts ensureInstance returns the fake auth.Registrar's InstanceID and the adapter exposes its Credential.
    - Token-reuse test asserts errors.Is(ensureInstance err, auth.ErrInstallTokenConsumed) is true.
    - Short-circuit test asserts a pre-set Auth.InstanceID yields zero registrar Register calls in BOTH modes.
    - `grep -v '^#' internal/agent/registration.go | grep -c "runtime.GOOS"` is non-zero (Platform sourced from runtime, not hardcoded).
    - localRegistrar is unchanged (mode: local path preserved).
  </acceptance_criteria>
  <done>registration.go selects the remote auth registrar for mode: remote (default) and keeps localRegistrar for mode: local; the install-token-consumed error propagates through ensureInstance — the R-54 selection contract is met.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AgentConfig.XDREndpoint + resolveIdentity wiring + RefreshCredential method + example.yaml</name>
  <files>internal/agent/agent.go, config/agent.example.yaml</files>
  <read_first>internal/agent/agent.go, internal/agent/registration.go, internal/auth/state.go, internal/auth/remote.go, internal/secrets/store.go, config/agent.example.yaml</read_first>
  <behavior>
    - AgentConfig gains an explicit field `XDREndpoint string \`mapstructure:"xdr_endpoint"\`` — the XDR
      base URL used for registration AND credential refresh (e.g. "https://xdr.company.com:8080"). The
      argusxdr OutputConfig.Endpoint is NOT scraped for this (BLOCKER 1, locked).
    - resolveIdentity(ctx, cfg, store, registrar-seams) resolves the agent identity PERSISTED-STATE-FIRST:
      (1) if a store is present, auth.LoadIdentity; if a persisted InstanceID exists, reuse it (and its
      Credential), no Register call (idempotent restart); (2) else if cfg.Auth.InstanceID is set,
      short-circuit registration and (in remote mode) persist it for next start; (3) else build the
      mode-aware registrar and call ensureInstance, then persist on success and clear the consumed
      InstallToken. On ErrInstallTokenConsumed with an existing persisted InstanceID, keep the persisted
      one and do NOT overwrite; with no persisted InstanceID, return the error.
    - mode: remote reads cfg.Agent.XDREndpoint and passes it to auth.NewRemoteRegistrar; if
      cfg.Agent.Mode == "remote" AND cfg.Agent.XDREndpoint == "", resolveIdentity returns a clear config
      error (e.g. "remote mode requires agent.xdr_endpoint").
    - WARNING 4 master-key gating: mode: local resolves via localRegistrar and does NOT construct a
      secrets.Store or require ARGUS_MASTER_KEY. mode: remote persists identity and therefore requires
      ARGUS_MASTER_KEY (or an injected key); the store is only created on the remote path.
    - Agent.RefreshCredential(ctx context.Context) error is a reachable runtime method: it builds the
      remote CredentialRefresher from cfg.Agent.XDREndpoint via auth.NewRemoteCredentialRefresher, loads
      the current Identity from the store, calls Refresh, and on success replaces the stored credential
      via auth.ReplaceCredential. It returns a clear error if mode != remote, if xdr_endpoint is empty,
      or if no persisted Identity exists. (Automatic refresh-before-expiry SCHEDULING is deferred to
      Phase 7 — only the reachable call path is added here.)
    - The agent provides injectable seams (store path / store, registrar, refresher base URL or a built
      refresher) so Task 4's tests drive register/refresh against an in-package mock WITHOUT a live XDR.
  </behavior>
  <action>
    Edit internal/agent/agent.go. (1) Add the XDREndpoint field to AgentConfig with the
    `mapstructure:"xdr_endpoint"` tag and a doc comment ("XDR base URL for registration + credential
    refresh; required in mode: remote; NOT the per-output signal endpoint"). (2) Introduce a
    resolveIdentity helper implementing the persisted-state-first + mode + master-key precedence above.
    Structure it with injectable seams so tests need no live XDR and no env var on the local path —
    prefer a small struct of dependencies, e.g. resolveIdentity(ctx, cfg, store *secrets.Store,
    makeRegistrar func() agent.Registrar) or an unexported agent field holding the store + an injectable
    registrar, mirroring how 04-05 injected the registry/fakes. The store is built only on the remote
    path (NewStore(StateFile, key-from-env) in Run/start) so local mode never touches NewStore/the
    master key. For mode: remote, build the registrar via NewRemoteRegistrarAdapter(
    auth.NewRemoteRegistrar(cfg.Agent.XDREndpoint, nil), cfg.Agent.InstanceName, agentVersion) and return
    a clear error when cfg.Agent.XDREndpoint == "". On successful registration build Identity{GroupID,
    InstanceID, Credential-from-adapter} and SaveIdentity, then clear cfg.Auth.InstallToken. (3) Replace
    ONLY the registration block of start() (currently agent.go:170-176, the
    ensureInstance(ctx, a.cfg, &localRegistrar{}) call) to call resolveIdentity and set a.instanceID;
    leave the registry/dispatcher/buffer/ingest/stop code untouched. Store the resolved store handle on
    the agent (remote mode) so RefreshCredential and tests can reach it. (4) Add the method
    Agent.RefreshCredential(ctx) error: guard mode==remote and xdr_endpoint!=""; build refresher :=
    auth.NewRemoteCredentialRefresher(cfg.Agent.XDREndpoint, nil); id, ok, err := auth.LoadIdentity(
    store); if !ok return a clear "no persisted identity" error; newCred, err := refresher.Refresh(ctx,
    id); on success auth.ReplaceCredential(store, newCred). (5) Edit config/agent.example.yaml: add an
    `xdr_endpoint` key under `agent:` with a comment explaining it is the XDR base URL for registration
    + credential refresh, required in remote mode, distinct from the per-output endpoint. Tests for this
    task's behavior live in Task 4 (same plan/wave). NO new module. Do NOT use -race.
  </action>
  <verify>
    <automated>cd /c/Users/Drupad/argus-sdk && go build ./... && go vet ./internal/agent/...</automated>
  </verify>
  <acceptance_criteria>
    - go build ./... clean; go vet ./internal/agent/... clean.
    - `grep -v '^#' internal/agent/agent.go | grep -c "XDREndpoint"` is non-zero (explicit config field added).
    - `grep -c "xdr_endpoint" config/agent.example.yaml` is non-zero (documented in example config).
    - `grep -v '^#' internal/agent/agent.go | grep -c "RefreshCredential"` is non-zero (reachable refresh entry point exists).
    - `grep -v '^#' internal/agent/agent.go | grep -c "NewRemoteRegistrar"` is non-zero (remote registrar built from XDREndpoint).
    - `grep -v '^#' internal/agent/agent.go | grep -c "NewRemoteCredentialRefresher"` is non-zero (refresher built from XDREndpoint).
    - `grep -v '^#' internal/agent/agent.go | grep -c "LoadIdentity"` is non-zero (persisted-state-first).
    - `grep -v '^#' internal/agent/agent.go | grep -c "SaveIdentity"` is non-zero (registered identity persisted).
    - The argusxdr OutputConfig.Endpoint is NOT used as the registration base URL (registration reads cfg.Agent.XDREndpoint only).
  </acceptance_criteria>
  <done>AgentConfig has an explicit xdr_endpoint field (documented in example.yaml); resolveIdentity resolves identity persisted-state-first, builds the remote registrar from cfg.Agent.XDREndpoint (clear error when empty in remote mode), and gates the secrets store/master key to remote mode only; Agent.RefreshCredential is a reachable entry point building the refresher from xdr_endpoint and replacing the stored credential — BLOCKER 1, BLOCKER 2 call path, and WARNING 4 gating are wired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Agent-package mock XDR + integration tests (register→persist→reload, refresh, single-use, local-no-key)</name>
  <files>internal/agent/agent_test.go</files>
  <read_first>internal/agent/agent.go, internal/agent/registration.go, internal/auth/state.go, internal/auth/remote.go, internal/secrets/store.go</read_first>
  <behavior>
    - The internal/agent tests stand up their OWN small httptest mock XDR (CROSS-PACKAGE GOTCHA:
      05-01's newMockXDR is in internal/auth/*_test.go and is NOT importable here). The agent-package
      mock serves POST /api/v1/sdk/register and POST /api/v1/sdk/credential/refresh over
      httptest.NewTLSServer, tracks consumed install tokens (409 on reuse), returns a deterministic
      InstanceID/Credential, and counts /register hits.
    - Register happy-path: resolveIdentity with no persisted state registers via the mock, persists, and
      a SECOND resolveIdentity with a NEW Store on the same state path returns the same InstanceID while
      the mock records exactly ONE /register call (reload-across-restart, no re-register).
    - Refresh: Agent.RefreshCredential against the mock rotates the credential, and a subsequent
      auth.LoadIdentity returns the NEW credential (asserts the endpoint is hit and the stored credential
      is replaced).
    - Install-token single-use: after a successful register, a second resolveIdentity whose registrar
      reuses the consumed token surfaces the typed error path but leaves the persisted InstanceID intact
      (LoadIdentity still returns the original).
    - xdr_endpoint config error: mode: remote with cfg.Agent.XDREndpoint == "" makes resolveIdentity (or
      start) return a clear config error; with a set xdr_endpoint the registrar is constructed against
      that base URL (assert the mock's URL is reached).
    - Master-key gating (WARNING 4): mode: local resolves identity WITHOUT ARGUS_MASTER_KEY set (no
      store, no master key) and makes no HTTP request to the mock; mode: remote requires the key
      (documented; tests inject/generate a key for the remote path).
  </behavior>
  <action>
    Create/extend internal/agent/agent_test.go. Implement a newAgentMockXDR(t) helper local to the agent
    test package: an httptest.NewTLSServer with an http.ServeMux routing /api/v1/sdk/register and
    /api/v1/sdk/credential/refresh, a mutex-guarded consumed-token map (StatusConflict on reuse), a
    /register counter, and deterministic InstanceID/Credential responses; the refresh handler returns a
    rotated credential. Build the agent's remote registrar/refresher against mock.URL by setting
    cfg.Agent.XDREndpoint = mock.URL and passing the mock's TLS client (mock.Client()) through the
    injectable http.Client seam (or inject the constructed registrar/refresher directly via the Task 3
    seam). Use t.TempDir() for the state path and secrets.GenerateMasterKey decoded to 32 bytes for the
    remote store. Write tests BEFORE Task 3's implementation is finalized where possible (TDD ordering),
    all driven by the local mock: (i) reload-across-restart: two resolveIdentity invocations on the same
    state path return the same InstanceID and the mock counter shows exactly ONE /register; (ii) refresh:
    call Agent.RefreshCredential and assert LoadIdentity returns the rotated credential AND the mock's
    /refresh handler was hit; (iii) single-use: second attempt reusing the consumed token returns the
    typed error and LoadIdentity still returns the original InstanceID; (iv) xdr_endpoint empty in remote
    mode returns a clear config error, and a set endpoint reaches mock.URL; (v) mode: local resolves via
    localRegistrar with ARGUS_MASTER_KEY unset (use t.Setenv to ensure it is empty), no store
    constructed, and zero HTTP requests to the mock. Do NOT use -race.
  </action>
  <verify>
    <automated>cd /c/Users/Drupad/argus-sdk && go test ./internal/auth/... ./internal/agent/... -count=1 && go vet ./... && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - internal/auth and internal/agent test suites pass Docker-free; go build ./... clean; go vet ./... clean.
    - The agent-package mock XDR is defined inside internal/agent test files (does NOT import 05-01's internal/auth mock).
    - Reload-across-restart test asserts the mock recorded exactly ONE /register call across two resolveIdentity invocations on the same state path and both returned the same InstanceID.
    - Refresh test asserts Agent.RefreshCredential hits the mock's /refresh endpoint AND LoadIdentity returns the rotated credential after the call.
    - Install-token single-use test asserts the second attempt surfaces the typed error AND LoadIdentity still returns the original InstanceID (not overwritten).
    - Config-error test asserts remote mode with empty xdr_endpoint fails with a clear error; with a set xdr_endpoint the registrar is constructed against that base URL (mock.URL reached).
    - Local-mode test asserts mode: local resolves identity with ARGUS_MASTER_KEY unset (no store, no master key) and makes no HTTP request to the mock.
    - go.mod gains no new module (stdlib net/http/httptest only).
  </acceptance_criteria>
  <done>An agent-package httptest mock XDR (independent of 05-01's mock) drives register→persist→reload-across-restart (exactly one /register), refresh via Agent.RefreshCredential (endpoint hit + credential replaced), install-token single-use protecting persisted state, the xdr_endpoint config-error path, and local-mode resolution without ARGUS_MASTER_KEY — R-54, R-52 reload, and the persistence/refresh/token-reuse half of R-55 met, with BLOCKER 2 refresh reachability proven and WARNING 4 gating verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → disk (agent-state.json) | InstanceID + Credential persist to local disk and are read back on restart |
| operator config → start() | Mode + Auth (InstallToken/InstanceID) + xdr_endpoint drive which registrar runs and what gets persisted |
| agent → XDR registration/refresh | InstallToken / InstanceID / Credential cross the network (TLS 1.3 client from 05-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Information disclosure | credential/InstanceID at rest | mitigate | Persisted only via secrets.Store (AES-256-GCM, 0600 file); a plaintext-on-disk negative test asserts the values never appear in the raw file |
| T-05-06 | Tampering | persisted-state file tampering | mitigate | secrets.Store uses AES-256-GCM with an auth tag; LoadSecrets returns a decryption error on tampering, which LoadIdentity propagates — the agent fails closed rather than trusting a forged InstanceID |
| T-05-07 | Spoofing / replay | install-token reuse overwriting identity | mitigate | On ErrInstallTokenConsumed with an existing persisted InstanceID, resolveIdentity keeps the persisted identity and refuses to overwrite it — a replayed/stolen consumed token cannot hijack a registered agent |
| T-05-08 | Elevation of privilege | self-reported InstanceID | mitigate | InstanceID is server-assigned via the remote registrar (mode: remote default) and persisted; it is never derived client-side in remote mode (localRegistrar's deterministic hash is confined to mode: local) |
| T-05-09 | Information disclosure | TLS downgrade on register/refresh | mitigate | The HTTP client comes from 05-01 (connector.NewTLSConfig, TLS 1.3, no InsecureSkipVerify); this plan adds no alternate cleartext path |
| T-05-10 | Tampering / misconfig | wrong registration endpoint | mitigate | Registration base URL is the explicit, documented cfg.Agent.XDREndpoint (not scraped from per-output config); remote mode with an empty xdr_endpoint fails fast with a clear config error rather than silently registering nowhere |
| T-05-SC | Tampering | dependency supply chain | mitigate | No new module; persistence reuses internal/secrets and registration reuses 05-01's stdlib net/http client — verified by the go.mod no-new-module gate |
</threat_model>

<verification>
cd /c/Users/Drupad/argus-sdk && go build ./... && go vet ./... && go test ./internal/auth/... ./internal/agent/... -count=1 -timeout 180s
</verification>

<success_criteria>
- R-52: InstanceID + Credential persist to agent-state.json encrypted via secrets.Store (AES-256-GCM, never plaintext) and reload across a simulated restart; credential refresh replaces the stored credential via the store's write-then-rename (atomic on POSIX, best-effort on Windows — do NOT claim hard Windows atomicity in the SUMMARY).
- R-54: agent.ensureInstance/resolveIdentity uses the real remote registrar by default in mode: remote and the existing localRegistrar in mode: local; a pre-set or persisted InstanceID short-circuits re-registration.
- R-55 (persistence half): the agent-package mock XDR (independent of 05-01's mock) backs tests for persist+reload-across-restart (exactly one /register call), credential refresh via Agent.RefreshCredential, and install-token single-use invalidation that leaves persisted state intact.
- BLOCKER 1: AgentConfig has an explicit xdr_endpoint field (documented in agent.example.yaml); resolveIdentity reads cfg.Agent.XDREndpoint for the registration base URL and returns a clear error when it is empty in remote mode. The argusxdr OutputConfig.Endpoint is NOT scraped.
- BLOCKER 2: Agent.RefreshCredential(ctx) is a reachable runtime entry point that builds the refresher from cfg.Agent.XDREndpoint, refreshes with the current Identity, and replaces the stored credential. NOTE against this criterion: automatic refresh-before-expiry SCHEDULING is OUT OF SCOPE (deferred to Phase 7) — only the call path is wired and reachable here; the verifier should treat scheduling as deferred, not missing.
- WARNING 4: mode: local resolves identity without ARGUS_MASTER_KEY (no store constructed); mode: remote requires ARGUS_MASTER_KEY (or an injected key) as a startup precondition.
- No new runtime dependency; persistence via internal/secrets; remote client via 05-01's TLS 1.3 net/http.

SUMMARY guidance (WARNING 6): the SUMMARY must describe secrets.SaveSecrets' replace as atomic on
POSIX and best-effort on Windows — do not overstate hard atomicity. Reusing the store is the correct
locked choice regardless.
</success_criteria>

<output>
Create .planning/workstreams/ws-c-v1-release/phases/05-xdr-registration/05-02-SUMMARY.md when done
</output>
</content>
