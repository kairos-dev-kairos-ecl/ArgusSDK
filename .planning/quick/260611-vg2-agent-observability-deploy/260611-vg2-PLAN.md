---
quick_id: 260611-vg2
slug: agent-observability-deploy
type: quick
date: 2026-06-11
description: Operability quick-wins — observability HTTP server (/healthz, /readyz, /metrics) + deploy artifacts (Dockerfile, sample agent.yaml, k8s Deployment)
---

<objective>
Add operability quick-wins to argus-agent: an observability HTTP server and
deployment artifacts. No new runtime dependency — the metrics endpoint hand-rolls
the Prometheus text format from the existing dispatcher Stats() map.
</objective>

<tasks>

<task>
  <name>Task 1: Observability HTTP server wired into agent lifecycle</name>
  <files>internal/agent/observability.go, internal/agent/observability_test.go, internal/agent/agent.go</files>
  <action>
    Add internal/agent/observability.go:
      - ObservabilityConfig{Disabled bool; Addr string} (mapstructure "observability"); default Addr 127.0.0.1:9090.
      - obsServer with *http.Server, atomic ready flag, statsFn func() map[string]uint64.
      - /healthz → 200 "ok" (liveness, always once serving).
      - /readyz → 200 when ready, 503 otherwise.
      - /metrics → Prometheus text exposition format for accepted/delivered/failed/dropped counters.
      - encodeMetrics(map) string — deterministic (sorted), HELP+TYPE+value per counter, named argus_dispatch_<key>_total.
      - start() Serve in goroutine; stop(ctx) graceful Shutdown.
    Add Observability field to Config. Add obs field to Agent. Start server in start()
    (ready=false), flip ready=true after collectors start. Shut down first in stop().
  </action>
  <verify>go test ./internal/agent/ -count=1 && go build ./... && go vet ./internal/agent/...</verify>
  <done>Agent serves /healthz, /readyz, /metrics; metrics encoding + handlers unit-tested; readiness green only after full start.</done>
</task>

<task>
  <name>Task 2: Deployment artifacts</name>
  <files>Dockerfile, deploy/agent.yaml, deploy/kubernetes/deployment.yaml</files>
  <action>
    Multi-stage Dockerfile (golang build → distroless/minimal runtime) building cmd/argus-agent.
    deploy/agent.yaml: documented sample covering agent.group_id, outputs (argusxdr+kafka examples),
    tls, ingest.listen, buffer, observability.addr.
    deploy/kubernetes/deployment.yaml: minimal Deployment with livenessProbe→/healthz,
    readinessProbe→/readyz on the observability port, config mounted from a ConfigMap.
  </action>
  <verify>go build ./... (Dockerfile/yaml are static artifacts — lint by inspection)</verify>
  <done>Deployable image definition, sample config, and k8s manifest with probes wired to the new endpoints.</done>
</task>

</tasks>

<success_criteria>
- /healthz, /readyz, /metrics served on a configurable addr (default 127.0.0.1:9090), gracefully stopped.
- Metrics in Prometheus text format from dispatcher Stats(); no new dependency.
- Unit tests for metrics encoding and health/ready handlers pass Docker-free.
- Dockerfile, sample agent.yaml, and k8s Deployment (probes wired) exist.
</success_criteria>
